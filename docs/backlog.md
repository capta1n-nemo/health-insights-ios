# The backlog — the one list

⚠️ **There is one open-item list in this repo and this is it.** Consolidated
2026-08-07 from three: this file, a generated roadmap table in
`docs/progress.md`, and two hand-written tables in `docs/activeContext.md`. A
ground-truth pass across all three found they **disagreed in twenty places**,
with **fifteen rows describing work that had already shipped**. `progress.md`
now keeps only the historical shipped narrative; `activeContext.md` keeps the
current state in prose. Neither records open work. **Do not start a second
list.**

**Do not read this file to find out what is open. Run the script:**

```bash
./scripts/backlog.sh --asks        # what the reader asked for and has NOT got
./scripts/backlog.sh --next        # the next batch, and the model it needs
./scripts/backlog.sh --tier mech   # everything at one complexity tier
./scripts/backlog.sh --id B14      # one row and its reasoning
```

**Rule: nothing gets removed, only marked.** An item that turns out to be
impossible becomes ❌ with a reason. An item that ships becomes ✅ with a commit.
Deleting a row is how a backlog starts lying.

## The row contract

<!-- backlog: prose-only — this section documents the contract itself. -->

Every item is one line of exactly this shape, with its reasoning in free prose
underneath. The prose is the point — the kept refusal reasons in §B5, the
calibration warning in §A3, the evidence warning on §B11 — and it is never
parsed. Only the header line is.

```
- `ID` STATUS `WAVE` `STREAM` `TIER` `gate:GATE` `ask` — **Title**
      Free prose, indented six spaces. Never parsed.
```

| Field | Values |
|---|---|
| **ID** | stable, never reused, never deleted |
| **STATUS** | ⬜ open · ◐ part done · ✅ shipped (+ commit) · ❌ will not be built (+ reason) |
| **WAVE** | `w0` blockers · `w1` shipped but wrong · `w2` quick wins · `w3` substantial builds · `w4` complex, last |
| **STREAM** | the workstream slug — `chrome`, `charts`, `sickdays`, `radar`, … |
| **TIER** | what model and effort it needs — see below |
| **GATE** | `none` · `phone` · `decision` · `external` · `needs:ID` |
| **`ask`** | present when the reader asked for this **in their own words** |

### The tier — what model each item needs

<!-- backlog: prose-only — this section documents the contract itself. -->

Work is batched by tier so a session changes model once and then grinds, rather
than stopping to ask. This is the reader's own instruction.

| Tier | Model and effort | What belongs in it |
|---|---|---|
| `mech` | **Opus 5 · medium** | exhaustive-switch sweeps, renames, one-line fixes, chevrons, pointers. No judgement. |
| `build` | **Opus 5 · high** | ordinary feature work — a section, a card, an `InputKind`, a `DataDomain`. |
| `hard` | **Opus 5 · xhigh/max** | calibration, statistics, anything moving a score's denominator, presentation rulings. |
| `ultra` | **Opus 5 + ultracode workflow** | research plus adversarial verification. |
| `design` | **Fable 5** | UI/UX shape and copy. |

⚠️ **Say the model out loud before starting a batch whose tier differs from the
one you are running.** `backlog.sh --next` prints it.

### Why the contract is rigid

`scripts/backlog.sh` **hard-errors** on a line that starts a row but does not
match, and on any section carrying table rows but no backlog rows. It refuses to
run rather than skipping quietly, and that is deliberate.

Its predecessor, `unbuilt-asks.sh`, was written to stop a three-session
*"where is my work?"* repeat. It matched two hard-coded headings and rows shaped
`| <digits> |`. The reader's 2026-08-07 brief (§B9–§B19) and their holidays
brief (§B7) are `##`-level with `H1`/`C1`/`S1` ids, so they were **structurally
invisible to it** — on the morning of 2026-08-07 it reported **two** open asks
while about thirty were open. **A parser that silently returns nothing reads as
"nothing outstanding."** That is the failure this contract exists to retire.

---

<!-- BACKLOG-INDEX:BEGIN — generated by scripts/backlog.sh, do not edit -->

**56 open** (40 not started, 16 part done) · 179 shipped · 1 will not be built. Regenerate with `./scripts/backlog.sh`.

| Wave | `mech` | `design` | `build` | `hard` | `ultra` | Total |
|---|---|---|---|---|---|---|
| **w0** Blockers — tooling and truth | 1 |  |  |  |  | 1 |
| **w1** Shipped but wrong | 2 |  | 2 | 2 | 1 | 7 |
| **w2** Quick wins | 10 |  |  | 1 |  | 11 |
| **w3** Substantial builds | 2 | 2 | 15 | 4 | 1 | 24 |
| **w4** Complex, last |  |  | 8 | 1 | 4 | 13 |

Gates on open rows: **phone** 15 · **none** 14 · **needs** 14 · **decision** 8 · **external** 5.

| # | Item | Wave | Tier | Gate |
|---|---|---|---|---|
| 1 | `AC2` Three things the reader must do on their own devices before daylight or fal… | w0 | `mech` | external |
| 2 | `D16` Score history is empty for five cards | w1 | `mech` | phone |
| 3 | `D61` The reader's export predates every model-improvement key — a fresh one is n… ⭐ | w1 | `mech` |  |
| 4 | `C11` Nothing has been seen with real calendar data in it | w1 | `build` | phone |
| 5 | `F1` Resting Heart Rate page cross-device defect | w1 | `build` | phone |
| 6 | `D54` Hangs, and nothing is watching for them ⭐ | w1 | `hard` |  |
| 7 | `D56` Sleep is not timezone- or travel-aware, and the reader has now actually flo… ⭐ | w1 | `hard` |  |
| 8 | `B19` The Energy card has no calibration — five invented constants set its whole… ⭐ | w1 | `ultra` |  |
| 9 | `B2-39` Verify Travel drain and Work impact against real calendar data | w2 | `mech` | phone |
| 10 | `D52` The diagnostics mirror is INFO-level, so everything but failures is hidden… | w2 | `mech` | decision |
| 11 | `D63` The app-target tests are flaky under concurrent load, and the failure names… | w2 | `mech` |  |
| 12 | `D9` Cold-launch time against the real 320,913-row record is unmeasured | w2 | `mech` | phone |
| 13 | `F2` Body Composition after the hatch change | w2 | `mech` | phone |
| 14 | `F3` Split-night proof from the next export | w2 | `mech` | phone |
| 15 | `F4` The ingestion pipeline | w2 | `mech` | phone |
| 16 | `F5` The cards on the phone | w2 | `mech` | phone |
| 17 | `F6` Phase 1 and Phase 2 sections | w2 | `mech` | phone |
| 18 | `F8` Screen Time bar measurement | w2 | `mech` | phone |
| 19 | `F7` Heart Health on a young profile | w2 | `hard` | phone |
| 20 | `B11-9` And: all of it reaches the data tab ⭐ | w3 | `mech` | needs:B11-4 |
| 21 | `B12-3` Activity tags become card candidates at the next review, not automatically ⭐ | w3 | `mech` | needs:B12-2 |
| 22 | `B16-3` Description (line 3): an AI summary of the whole card ⭐ | w3 | `design` | decision |
| 23 | `D29` The Data tab has grown very long | w3 | `design` | decision |
| 24 | `AC4` The delete-everything wipe must be driven by the Schema, not a hand-written… | w3 | `build` | needs:Q13 |
| 25 | `B11-2` The per-day page (reachable from either view) ⭐ | w3 | `build` | needs:B11-1 |
| 26 | `B11-3` Estimated sickness, which the reader can correct ⭐ | w3 | `build` | needs:B11-2 |
| 27 | `B11-5` The learning loop — and the reader is explicit about scale ⭐ | w3 | `build` | needs:B11-3 |
| 28 | `B2-18` Stress Tracking "like Oura" ⭐ | w3 | `build` | needs:N1 |
| 29 | `B9-2` Screen time card ⭐ | w3 | `build` | needs:B10-1,B10-2 |
| 30 | `F9` The share-sheet action extension (parked on signing) | w3 | `build` | phone |
| 31 | `H2` OOO / out-of-office handling ⭐ | w3 | `build` | needs:H1 |
| 32 | `H7` The leave recommendation ⭐ | w3 | `build` | needs:H5,H6 |
| 33 | `I2` Hume Band direct API | w3 | `build` | external |
| 34 | `I3` Ultrahuman | w3 | `build` | external |
| 35 | `I4` Garmin | w3 | `build` | external |
| 36 | `I5` Fitbit | w3 | `build` | external |
| 37 | `Q12` Write-back to Apple Health ⭐ | w3 | `build` |  |
| 38 | `S13` Trend the breathing-disturbance index | w3 | `build` | needs:S9 |
| 39 | `B11-8` Weightings ⭐ | w3 | `hard` | needs:B12-1 |
| 40 | `B12-2` Use AI to map tags to high-level categories — and it must scale to tags nev… ⭐ | w3 | `hard` | needs:B12-1 |
| 41 | `D40` Heart Health's one derived figure is computed in the view, so it cannot be… | w3 | `hard` | decision |
| 42 | `H6` Cards read the ledger ⭐ | w3 | `hard` | needs:H5 |
| 43 | `R60` Seven research briefs commissioned 2026-08-07 evening — collect their outpu… ⭐ | w3 | `ultra` |  |
| 44 | `B2-17` Cycle tracking — a whole fifth tab ⭐ | w4 | `build` | decision |
| 45 | `B5-37` Daylight/UV, spirometry, mindfulness, mood, oral health, falls ⭐ | w4 | `build` | decision |
| 46 | `D7` English only | w4 | `build` |  |
| 47 | `D8` No widgets, Live Activities or watch target | w4 | `build` |  |
| 48 | `I8` Barcode scanner with on-device lookup (Open Food Facts / USDA) | w4 | `build` | phone |
| 49 | `I9` Camera + LiDAR guided body scan | w4 | `build` | phone |
| 50 | `Q15` Body scanner ARKit capture ⭐ | w4 | `build` |  |
| 51 | `R16` Camera-based input (AI + LiDAR) is still the open half ⭐ | w4 | `build` |  |
| 52 | `D42` "How you compare" still has no norms for anything a card derives, and cannot | w4 | `hard` | decision |
| 53 | `B11-7` The fake-sick-day problem — the reader's own scientific objection, and the… ⭐ | w4 | `ultra` | decision |
| 54 | `N1` Stress, done properly ⭐ | w4 | `ultra` |  |
| 55 | `P23` Signal audit ⭐ | w4 | `ultra` |  |
| 56 | `R57` Core ML personal anomaly detection once enough history exists | w4 | `ultra` |  |

⭐ = the reader asked for this in their own words.

<!-- BACKLOG-INDEX:END -->

---

---

## 0. Start here next session

⚠️ **2026-08-06, second message: the reader answered all 24 open questions and
reversed 11 of the 12 refusals in one go.** §A is now a specification rather than
a blocker, and §B5 is now a build list. **Read §A and §B5 before N1–N3 below** —
they were written when a dozen things were still "no".

**Where the build order went.** This section used to carry a nine-row "build
order the answers imply" table, most-wanted first. It is gone, deliberately: it
had drifted — its rows 6 and 7 still listed the fitness sections and the
sound-exposure work as outstanding when §B5 #34/#35 record them shipped and the
code agrees (`EffortIntensityModel.swift` exists and `FitnessInsight.swift`
references it) — and every row of it duplicated an id that already lives in §B5.
**Ordering now lives in each row's `WAVE` field and is rendered by
`./scripts/backlog.sh`.** That is the generated index, and unlike a hand-written
table it cannot disagree with the rows it orders.

What the table recorded, kept because the commits are worth having: **own-brand
biological age** shipped `972e2d7` (§B5 #29); **cuffless BP, ungated** shipped
`0ea9411` (§B5 #28); **the mental health card** shipped `867129e` (§B5 #27);
**the cycle tab** shipped in two slices — `a8be5ae` (the log, the calendar, the
length range) and slice 2 (phase model, fertile window, phase-aware baselines,
Oura temperature channel) — with ⚠️ **one thing deliberately unfinished that is
not a bug**: the phase-aware baselines are built and tested but **not wired into
the symptom radar**, see §A3 (§B5 #31); **score decomposition in the deep dive**
shipped `964c03e`, on `ScoreComparisonDetailView` (§B5 #38, S2); and **the
fitness sections** — intensity feeding the score, steps/distance/flights —
shipped `5ed4ac7` (§B5 #34–#35), corrected 2026-08-07 against the code after the
table had claimed otherwise. The remaining rows only pointed elsewhere, and
those pointers are still the authority: sound exposure at §B5 #33 (◐ — the two
dose metrics shipped, the card remains) and sleep apnoea at §B5 #30 (⚠️
**identifiers now requested — count the rows before building**), with radar
accuracy ✅ `44fb94e` at §B5 #36; notifications, delete-everything, the export
gaps and the calendar at `Q10`–`Q13` and `I1`; and capture for the six zero-row
domains at §B5 #37.

- `N1` ◐ `w4` `stress` `ultra` `gate:none` `ask` — **Stress, done properly**
      **The reader's instruction, 2026-08-06:** *"I want to rename it to stress,
      and research how Oura ring do this, because they track stress, but lets do
      it better because we have more data."*

      The rename shipped in `0dbc9b6` — "Sustained load" → **"Stress load"**, and
      the balance web's spoke reads "Stress". That was the trivial half. The
      research is the item.

      ⚠️ **Oura's own stress data is already in the raw catalogue and read by
      nothing.** Measured in the reader's export:

      | Field | Coverage |
      | --- | --- |
      | `oura.daily_stress.day_summary` | 142 days, 90 of the last 90 |
      | `oura.daily_stress.stress_high` | same |
      | `oura.daily_stress.recovery_high` | same |
      | `oura.daily_resilience.level` | present |
      | `oura.daily_resilience.contributors.stress` | present |
      | `oura.daily_resilience.contributors.daytime_recovery` | present |
      | `oura.daily_resilience.contributors.sleep_recovery` | present |

      So the comparison is free: **this app's stress score can be graded against
      Oura's own, day by day, on 90 of the last 90 days.** No competitor can do
      that about itself.

      What to establish before building:

      1. **What Oura actually measures.** Their stress is a *daytime* signal — it
         reads the day, not the night. This app's Stress load reads four
         *nocturnal* channels over 28 days against 90. Those are different
         quantities, and the first question is whether the reader wants the
         daytime one, the sustained one, or both on one card.
      2. **What we have that they do not.** Continuous daytime heart rate, the
         medication schedule and its modelled level, the substance log, cuff
         blood pressure, gait, and — once built — the calendar. Oura has none of
         that. The "do it better" claim has to name which of these carries the
         advantage rather than asserting more data is better.
      3. ⚠️ **`daily_stress` is a vendor composite with an undisclosed formula.**
         It may be *shown* as a labelled second opinion; it must never be blended
         into this app's own score, on the same rule that governs `vascularAge` —
         relay, never merge.
      4. **The overlap question, again.** Readiness answers *this morning*; the
         symptom radar answers *is something acute converging*; Stress load
         answers *has this lasted weeks*. A daytime stress signal is a fourth
         window and has to justify itself against all three, or it is a fourth
         rendering of one measurement.
      5. **The naming lesson is part of the item.** The reader asked for a stress
         card, it shipped under a name they could not find, and they asked three
         more times. Whatever this becomes, it is findable by the word they think
         in.

      ⚠️ **Gated on a ruling the code cannot make** (2026-08-07): daytime signal,
      sustained nocturnal one, or both on one card — and the word the reader
      thinks in, since the last stress card shipped under a name they could not
      find and they asked three more times. Point 1 and point 5 are the gate,
      not background reading.

      ✅ **RULED 2026-08-07: both, on one card.** A daytime signal AND the
      sustained nocturnal one, with **Oura's own number beside ours as a
      labelled second opinion, never blended in**. The reader's words settle
      point 1 of the research list above; points 2–5 still bind.
      ⚠️ **The design produced against this ruling was REFUTED** — see
      `docs/stress-design-2026-08-07.md`, which carries a not-build-ready
      banner. Three hostile reviewers returned `needs-rework`. **The ruling
      stands; the design has to be redone.**

- `N2` ✅ `w3` `calendar` `build` `gate:none` — **The calendar integration is persisted, not just fetched**
      Original entry, kept because it still shapes the wording: *"N2 — the
      calendar integration (§D, I1), because it is the single thing that unblocks
      two requested cards (travel drain, work impact) and feeds N1's point 2."*

      ⚠️ **That is false as of `77847b2`, verified against the code 2026-08-07.**
      `CalendarEventRecord` (`PersistenceModels.swift:232`) and
      `CalendarJudgementRecord` (`:349`) are both `@Model`s, both registered in
      `DataStore.swift:21`'s `Schema`, written by `DataStore.mergeCalendarEvents`
      (`:485`) from `AppModel.syncCalendar` (`:350`), read back by
      `loadCalendarEvents` (`:473`) into `AppModel.calendarEvents` (`:249`), and
      bound into the engine at `InsightEngine.swift:89-93`. **N2 is not a blocker
      for anything.** The two cards it was said to unblock are also built — see
      §B2 #15 and #16 — and only real-data verification on the reader's phone
      remains for either.

- `N3` ✅ `w3` `rules` `build` `gate:none` `ask` — **A bespoke section on every card**
      Original entry, kept because it still shapes the wording: *"N3 — a bespoke
      section on every card (§C, S1), the reader's standing rule from 2026-08-06,
      with the design docs updated so it is enforced rather than remembered. Five
      cards have none today."*

      ⚠️ **The "five cards" claim is false, verified against the code
      2026-08-07.** `InsightDetailView.swift:79-160` is exhaustive over all 26
      `InsightID` cases and renders `gaitDecompositionCard`,
      `sustainedLoadChannelsCard`, `micronutrientCard` and `energyBalanceCard`.
      Readiness is the single deliberate `EmptyView` with a documented reason —
      its picture is the seventeen-vital strip, drawn universally. N3, §C `S1`,
      §E `D18` and §E `D1` are four rows describing one already-closed item.

---

## A. Every open question — ✅ ALL 24 ANSWERED 2026-08-06

**The reader answered every one in a single message. Nothing in §A blocks work any
more; every row below is now a specification. Where the reader asked a question
back, my answer is recorded beside theirs — that exchange is the decision and
must not be re-opened.**

⚠️ **The framing that governs all of it, in the reader's own words:**

> *"I want to derive new insights from the data people already have. I am not
> just looking to copy — I want to be ground breaking and unique, we are going to
> do stuff that others will not do. I do not care about compliance at this point,
> it's my personal project for now."*

**So the compliance/clearance objection is retired as a reason to refuse a
feature.** It stays as a reason to *word* a feature carefully — the app still
never dresses modelled as measured (rule 3 in `architecture.md`), and it still
states its own error. What it no longer does is decline to compute something
because a regulator would want a trial first. This is one person's private
project. If that changes — distribution, an App Store listing, a second user —
**§B5's original reasons all come back**, which is why they are preserved below
rather than deleted.

⚠️ **These are answers, not items.** Everything in §A1, §A3, §A4 and §A5, plus
Q9 and Q14 in §A2, stays as prose on purpose: they are settled rulings, and a row
is something a session can pick up and build. **Nothing in §A may be re-asked**,
and no row anywhere in this file may carry `gate:decision` pointing back at a
question §A already answered on 2026-08-06.

### A1 — Decisions on things already built

<!-- backlog: prose-only — answered decisions, not work. Kept as prose so they cannot be re-asked. -->

| # | Question | ✅ Decision |
|---|---|---|
| Q1 | Substance card at 4 episodes: honest version or wait? | **Ship the honest version.** Per-episode deltas, the named alternative explanation beside each row, no score, and the sentence "nothing has happened the same way often enough to tell it from an ordinary run" |
| Q2 ✅ | BP card shows two ± and two cuff ages on one screen. **Shipped `0ea9411`** | Reader asked *"what do you mean?"* — fair, the row was written for someone looking at the screen. **What it means:** the card prints "±14, fitted to 23 readings" in one place and "the ±13 it is judged on" a few inches below, and "over a day old" beside "2 days ago", so it contradicts itself twice on one screen. **Decision (mine, stated so it is not re-asked): show one ± — the one the estimate is actually judged on — and one cuff age.** The other becomes a line in the detail sheet |
| Q3 ✅ | Fitness age: VO₂ 30 → a 68-year-old's fitness. Right? **Shipped `5330d92`** | **"Honestly doesn't seem right."** Confirmed. Below VO₂ ~36 the norm table is extrapolation off its lowest anchor (32 at 65). **Floor the extrapolation and widen the stated ±.** Moves `FitnessInsight` *and* `HeartHealthScore.vo2Score` |
| Q4 ✅ | Micronutrients: score now or wait? **Shipped `5330d92`** | **Estimate now, state the limitation, name the data that would sharpen it.** Wires `MicronutrientTargets`, which is currently dead code, and makes the Nutrition card's mandatory sex/DOB ask true instead of false |
| Q5 ✅ | Feedback gated so Nutrition/Metabolism cannot be rated. **Shipped `5330d92`** | **Ungate.** One line |

⚠️ **Two of these have been re-verified in the code, 2026-08-07, and the doubts
recorded elsewhere about them are wrong.** Q4 landed: `MicronutrientTargets` is
referenced in three files now and is no longer dead code (which also means §E
`D4`'s "six dead modules" is five). Q5 landed:
`InsightDetailView.swift:3669-3683` branches the feedback *wording* on
`primaryValue == nil` rather than hiding the affordance, so the cards most in
need of correction can be corrected — that is §E `D3`, closed.

### A2 — Decisions on things not yet started

Ten questions were answered here. **Two of them settle a posture and name no
build of their own**, so they stay as prose where they cannot be picked up as
work and cannot be re-asked:

**Q9 — Mental health: build the computes-nothing Mind section?** ✅ **No — build
a real card.** *"I want a mental health card. Figure it out, creative licence +
data science."* See §B5 #27, reversed — shipped `867129e`.

**Q14 — Does the app stop launching when the free-team profile expires?** ✅
Reader: **"No?"** — reported as not observed. ⚠️ Treated as *unconfirmed*, not as
settled: a free-team profile is documented as 7 days, the app has never gone that
long without a deploy, so nobody has actually tested the case. **Left as a watch
item, not as work** — and deliberately given no row, because there is nothing to
build and the only thing that could ever falsify it is the reader's own phone
going a week without a deploy.

The other eight name work:

- `Q6` ✅ `w4` `capture` `build` `gate:none` `ask` — **Location: feed and prompt together**
      Asked as *"Location: feed first, or both?"* ✅ **"Just build the whole
      thing."** Feed and prompt together.

- `Q7` ✅ `w4` `labs` `build` `gate:none` `ask` — **Bloods: every input route, not a choice between two**
      Asked as *"Bloods: manual entry or PDF/OCR?"* ✅ Reader: *"both? What do you
      mean? We should be able to accept all of these."* **Decision: all input
      routes for a blood result — typed, PDF, photographed report, and the
      existing document scanner.** The question was a false choice and should not
      have been asked as one.

- `Q8` ✅ `w4` `nutrition` `build` `gate:none` `ask` — **Supplements: capture the stack, sum it ingredient by ingredient**
      Asked as *"Supplements: worth the one-time capture?"* ✅ Reader: *"Yes? From
      where?"* — **the reader enters them**, by label scan or by typing, because
      no wearable or health store carries a supplement stack. Then summed
      **ingredient by ingredient** against published upper limits, which is the
      part nobody ships. NIH DSLD (200,000+ labels, free API) supplies the
      ingredient lists.

- `Q10` ✅ `w2` `export` `build` `gate:none` — **The four remaining export fields**
      ✅ **Tokens done (`964c03e`): `OAuthTokens` is no longer `Codable`, so a
      token cannot be a stored property of any `Encodable` type — a compile error
      rather than a convention.** The four missing fields are still to build.
      **Build them. Do not include tokens.** Connection state, suggestion
      dismissals, the feedback ledger and prediction outcomes are exported;
      credentials must be **structurally impossible** to serialise, not merely
      omitted.

- `Q11` ✅ `w4` `pipeline` `build` `gate:none` `ask` — **Notifications — background delivery first, then the alerts on top**
      Asked as *"Notifications — none exist anywhere"*. ⚠️ **Sequenced,
      2026-08-06.** There is no `BGTaskScheduler`, no `BackgroundTasks` import
      and no `UIBackgroundModes` in the repo, so anything built today fires
      **only in the foreground** — a radar flag at 3am could not reach anyone.
      **Reader's decision: build the background delivery first, then
      notifications on top.** A symptom-radar alert that only fires when you
      happen to open the app is not the feature that was asked for. **Build
      them.** Named by the reader: **symptoms**, and **when a card changes
      majorly**. Plus *"any other major things you think we should notify on"* —
      creative authority granted, so: a flagged radar episode opening or closing,
      a grounding fact going stale (a cuff reading the BP estimate now needs), a
      body-scan cadence due, and a connector that has stopped syncing.

- `Q12` ⬜ `w3` `integrations` `build` `gate:none` `ask` — **Write-back to Apple Health**
      ✅ **Wanted, but not yet. Roadmap.** Do not build this session.

      ⚠️ 2026-08-07: the reader wants this and parked it *explicitly*. It needs
      **scheduling, not re-asking** — do not put it back to them as an open
      question, and do not carry it as a decision the reader owes.

- `Q13` ✅ `w3` `data` `build` `gate:none` `ask` — **Delete-everything path**
      Asked as *"Delete-everything path"*. ✅ **Yes**.

- `Q15` ◐ `w4` `bodyscan` `build` `gate:none` `ask` — **Body scanner ARKit capture**
      Asked as *"Body scanner ARKit capture priority"*. ✅ **Yes** — build it.

### A3 — Cycle tracking: all four answered, so all ten items are unblocked

<!-- backlog: prose-only — answered decisions, not work. The build rows live in §B5 #31 and the cycle stream. -->

✅ **Built, both slices.** Slice 1 `a8be5ae` (the log, the calendar, the length
range). Slice 2: `CyclePhaseModel` and `PhaseAwareBaseline` in InsightKit, and
the tab now shows the phase with its ±, the fertile window on the calendar with
softened edges, and the reader's own per-phase shifts.

⚠️ **Machinery shipped, wiring blocked — read this before "finishing" it.**
`PhaseAwareBaseline` is **not** consumed by `HealthWatchModel`, the symptom
radar or `Baseline.deviation`, and that is a decision rather than an omission.
The named TODO is at
`InsightKit/Sources/InsightKit/Baseline/PhaseAwareBaseline.swift:29`
(`TODO(cycle-phase-baseline)`). Swapping the radar's whole-window reference for
a phase-aware one moves the denominator of every z-score it has ever recorded,
and its thresholds are **calibrated** — `leaningZ`, `strongZ`, `channelCap`,
`nullMean`, `assumedDependence` and the simulated false-alarm budget in
`SymptomRadarTests` are all stated against the current reference. **The wiring
and the recalibration are one commit or neither.** It also needs a decision the
code cannot make: what the radar does for a reader with no cycle log, which is
every reader on the current install.

The defect the wiring would fix is pinned as a test —
`PhaseAwareBaselineTests.testTheLutealPatternIsWhatTheRadarCallsIllness`
asserts the *current* behaviour, so it will fail when the fix lands. That
failure is the signal, not a regression.

⚠️ **The confirmation this needs is tracked as roadmap-39, not here.** §A3 is not
re-opening a §A question — the reader has to confirm that the phase-aware
baselines should now be wired in and recalibrated together, and that ruling
belongs to the roadmap row, so it is not duplicated as a second gate on this
section.

⚠️ **Q25, asked and answered 2026-08-06 — the fifth decision, and it was blocking
every line of code.** A scouting pass found the app is *structurally*
single-user: `ProviderCredentialStore` keys on `providerID` alone,
`IntegrationRecord.integrationID` is `@Attribute(.unique)` (one Oura account per
install), and `deploy.yml` reaches exactly one iPhone. The tab is for someone
else's body.

**Decision: she installs the app on her own phone, with her own Oura key.** The
cycle tab is therefore a *feature of a single-user app*, not a second-profile
concept — which is the cheapest of the three answers by a wide margin, keeps her
ring's temperature and HRV in scope (the whole reason this beats Flo), and means
nothing about the profile, the baselines or the cards has to change.

**What that rules out, and it must not be quietly re-opened:** no second
profile, no per-person baselines, no "whose data is this" on any sample. If a
future session finds itself adding a person dimension, that is a different
decision and needs asking again.

⚠️ **And it changes what the data claims mean.** Every zero-row and coverage
figure in this file — `MenstrualFlow` 0, `SexualActivity` 0,
`basalBodyTemperature` 136 — is measured against **the reader's** export.
Nothing here says anything about *her* ring. Do not write "already arriving"
about her record in a doc, a commit message, or on screen.

| # | Question | ✅ Decision |
|---|---|---|
| Q16 | Does the tab draw a fertile window at all? | **"YES! THAT'S THE WHOLE POINT."** The fertile window is the feature, not a stretch goal |
| Q17 | Surface the tirzepatide / oral-contraceptive labelling? | **Yes** |
| Q18 | Who is the tab for? | **The reader's wife.** *"She has an Oura ring, they want a huge amount of money every month for their feature. I want to build it instead since we have all the same data they do."* ⚠️ **This is the single most useful answer in the whole set** and it changes the design: the tab is for a specific person with a specific ring, the competitor is Oura's paid cycle insights (and Flo), and the win condition is *the same conclusions without the subscription* |
| Q19 | The privacy posture | Settled by Q20 |
| — | Zero rows in `MenstrualFlow` / `SexualActivity` | ⚠️ **Still true and still the hard part.** The tab ships with manual entry as a first-class path, and `basalBodyTemperature` (136 rows, 80 of the last 90) plus the ring's nocturnal temperature are what let a cycle be *confirmed* rather than guessed. **Note the data belongs to the wife's ring, not the reader's export** — coverage on her record is unmeasured |

⚠️ Q16 is **answered and was taken in code** — the fertile window is among slice
2's shipped items. Where the roadmap still carries "does the cycle tab draw a
fertile window at all?" as an open decision, that row needs *confirming and
closing*, not re-asking.

### A4 — Crowd-sourced norms

<!-- backlog: prose-only — answered decisions, not work. The build rows are in the norms stream. -->

| # | Question | ✅ Decision |
|---|---|---|
| Q20 | Opt-in per signal? | **"NO — opt-in for everything (by default) with ability to opt out."** So: **on by default, one global opt-out, no per-signal consent matrix.** ⚠️ This is a real change of posture — today nothing leaves the phone — and it is recorded here as the reader's explicit instruction for their own private build |
| Q21 | Contribute automatically once you consume, or separately? | Reader: *"What do you mean?????"* — the question was jargon. **What it meant:** if you use the crowd norms to see how you compare, does your own data automatically join the pool, or is joining a second, separate switch? **Answered by Q20: automatically, on by default, one opt-out covers both** |

### A5 — Small ones

<!-- backlog: prose-only — answered decisions, not work. -->

| # | Question | ✅ Decision |
|---|---|---|
| Q22 | Does MyFitnessPal already write into Apple Health for you? | **Yes.** So the food-capture integration is a Settings row saying so, not a build. ⚠️ But `dietaryEnergy` is 0 rows in the last 90 on the export — so it writes when the reader logs, and the reader has not been logging |
| Q23 | Travel drain: calendar enough, or timezone metadata too? | **"EVERYTHING, AS MUCH AS POSSIBLE"** — calendar *and* HealthKit timezone metadata, **plus a manual travel tag the reader can add** |
| Q24 | "Stress card like Oura" — something other than Sustained Load? | **"NO"** — Sustained Load (now "Stress load") is what was meant. N1's research item stands; the card is not missing |

---

## B. Every card ever mentioned

### B1 — Built and shipped (14)

- `B1-1` ✅ `w3` `newcards` `build` `gate:none` — **Readiness**
      Today · Scores

- `B1-2` ✅ `w4` `radar` `hard` `gate:none` — **Symptom radar**
      Today · Scores. Rebuilt 2026-08-05: calibrated statistic + CUSUM memory

- `B1-3` ✅ `w3` `sleep` `build` `gate:none` — **Sleep**
      Today · Scores

- `B1-4` ✅ `w3` `newcards` `build` `gate:none` — **Energy**
      Today · Scores

- `B1-5` ✅ `w3` `newcards` `build` `gate:none` — **Heart Health**
      Insights · Scores

- `B1-6` ✅ `w3` `newcards` `build` `gate:none` — **Fitness**
      Insights · Scores

- `B1-7` ✅ `w3` `newcards` `hard` `gate:none` — **Heart Attack & Stroke Risk**
      Insights · Scores. Carries the age-comparison section

- `B1-8` ✅ `w3` `bp` `hard` `gate:none` — **Blood Pressure**
      Insights · Scores. See Q2

- `B1-9` ✅ `w3` `newcards` `build` `gate:none` — **Body Composition**
      Insights · Scores

- `B1-10` ✅ `w3` `nutrition` `build` `gate:none` — **Nutrition**
      Insights · ⚠️ **Empty state — has never scored.** 0 logged food days in the last 90

- `B1-11` ✅ `w3` `nutrition` `build` `gate:none` — **Metabolism**
      Insights · ⚠️ **Empty state — has never scored.** Same cause

- `B1-12` ✅ `w3` `stress` `build` `gate:none` — **Sustained load**
      Insights · Scores. **This is the stress card** — the name is why it is hard to find

- `B1-13` ✅ `w3` `newcards` `build` `gate:none` — **How you walked**
      Insights · Scores. Gait, shipped 2026-08-05

- `B1-14` ✅ `w3` `substances` `build` `gate:none` — **Substance Impact**
      Insights · Scores. See Q1

- `B1-15` ✅ `w4` `newcards` `hard` `gate:none` — **Biological age**
      Insights · Scores. Shipped 2026-08-06. Its own bespoke section shows every marker's age, error and share

- `B1-16` ✅ `w4` `newcards` `hard` `gate:none` — **Mental health**
      Insights · Scores. Shipped 2026-08-06. ⚠️ Deliberately **off** the balance web — see §E D17

### B2 — You asked for them; they do not exist

- `B2-15` ✅ `w3` `calendar` `build` `gate:none` `ask` — **Travel drain**
      **Shipped.** ⚠️ Unverified with real data — The calendar integration now exists
      and `CalendarModel.timeZoneChanges` is the travel signal, tested. ⚠️ Still true
      that **the app captures no HealthKit metadata**, so a calendar event's own time
      zone is the only handle on where a reading was taken

      ⚠️ The blocker this row used to carry — *"What remains: persist the events, then
      the card"* — is stale. The events persist (`CalendarEventRecord`,
      `PersistenceModels.swift:232`), and `TravelDrainInsight` and `TravelDrainModel`
      both exist and read them (`CalendarInsights.swift:859`, `:620`). Only real-data
      verification on the reader's phone remains — that is B2-39.

- `B2-16` ✅ `w3` `calendar` `build` `gate:none` `ask` — **Work impact**
      **Shipped.** ⚠️ Unverified with real data — `CalendarModel.committedHours` and
      `busiestDay` are the input, tested.

      ⚠️ The blocker this row used to carry — *"Needs the events persisted, then the
      card"* — is stale. The events persist, the card shipped, and work-impact-v2
      scoring shipped with it. Only real-data verification on the reader's phone
      remains — that is B2-39.

- `B2-39` ⬜ `w2` `calendar` `mech` `gate:phone` — **Verify Travel drain and Work impact against real calendar data**
      What is left of B2-15 and B2-16 after both shipped: neither has ever been seen
      against the reader's own calendar. `CalendarModel.timeZoneChanges` and
      `CalendarModel.committedHours` / `busiestDay` are tested, and the persistence
      path is verified in code — what is not verified is what the two cards say on a
      real record. Only the phone can close it.

- `B2-17` ◐ `w4` `cycle` `build` `gate:decision` `ask` — **Cycle tracking — a whole fifth tab**
      ✅ The tab exists and logs. Four decisions answered plus Q25. ⚠️ **Zero rows is
      still true of the reader's export and says nothing about hers**

      ⚠️ Four rulings still gate the rest of the tab: the privacy posture stated on the
      tab itself (roadmap-45, explicitly a precondition); who the tab is keyed to —
      `biologicalSex`, an explicit setting, or the existence of any cycle data
      (roadmap-44); whether the app surfaces the tirzepatide / oral-contraceptive
      labelling it uniquely knows the dose dates for (roadmap-43); and who writes the
      Phase 5 content layer (roadmap-41), without which the tab is a chart and with
      somebody else's copy is a liability.

- `B2-18` ⬜ `w3` `stress` `build` `gate:needs:N1` `ask` — **Stress Tracking "like Oura"**
      Possibly already Sustained Load — see Q24

      ⚠️ What is unsettled is N1: daytime signal, sustained nocturnal one, or both on
      one card — and the word the reader thinks in, since the last stress card shipped
      under a name they could not find and they asked three more times.

### B3 — Proposed by the competitive research, not built

- `B3-19` ✅ `w3` `transparency` `build` `gate:none` — **"What I could see last night"**
      Which instruments actually reported, and which cards are therefore abstaining.
      **The strongest single idea in the whole scan.** Today a green radar is ambiguous
      between *nothing stirring* and *the ring was on the charger*. Oura's
      `non_wear_time` (81 of your last 90 days) sits unread as the denominator

- `B3-20` ✅ `w3` `sleep` `build` `gate:none` — **"When you settled"**
      Last night's overnight HR and HRV *curve* against your typical curve. Every
      product reports a summary; none draws the within-night shape. Probably a Sleep
      section

- `B3-21` ✅ `w3` `substances` `build` `gate:none` — **"What the drug is doing"**
      Every metric folded onto days-since-dose rather than the calendar, with the dose
      count on the figure. 14 tirzepatide doses, 9 side-effect records, 1,614 Withings
      weights

- `B3-22` ✅ `w3` `newcards` `build` `gate:none` — **"Sound you took on"**
      Cumulative headphone dose against the WHO/NIOSH budget. 13,768 rows over 467
      days. Must state the hours it could not see

- `B3-23` ✅ `w3` `transparency` `build` `gate:none` `ask` — **"Which instrument to believe"**
      Where Watch, ring and scale disagree — show both, say which the app used and why.
      **You asked for this again tonight.** #27 is now fixed so it will not draw a gap
      it creates itself

      ⚠️ Unsettled: a card or a section. §C S8 records it as a section, this row as a
      card, and the reader has asked for it again without settling that.

      ✅ **RULED 2026-08-07: both — sections plus an index card.** Per-card
      sections put the disagreement where the decision is made, which matches the
      reader's own placement of "why is my score low" (*an explanation one tap
      from the number is one people actually read*); the index card makes it
      findable in one place. Roughly double the work and chosen deliberately.
      **This settles the §B3 #23 / §C S8 duplication** — they were the same ask
      recorded as a card and as a section, and the answer is that it is both.

- `B3-24` ✅ `w3` `labs` `build` `gate:none` — **"Your bloods"**
      Manual lipid + HbA1c entry. See Q7

- `B3-25` ✅ `w3` `substances` `build` `gate:none` — **"What is actually in your stack"**
      Supplements summed ingredient by ingredient against upper limits. See Q8

### B4 — Built as a section rather than a card

- `B4-26` ✅ `w3` `newcards` `build` `gate:none` — **"How old does each thing think you are"**
      Heart Attack & Stroke Risk card, bottom. Every age estimate, attributed, each
      with its own **derived** error

### B5 — Previously refused. ⚠️ **ELEVEN OF TWELVE REVERSED BY THE READER, 2026-08-06.**

**The reader's ruling, verbatim:** *"There are other things that you've said no to
for stupid reasons like compliance or privacy — just do them."* And on #29
specifically: *"These sorts of things are the ENTIRE POINT OF THE APP."*

**Read this before re-refusing anything.** The original reasons are kept in the
right-hand column on purpose — not one of them was factually wrong, and each
still governs *how* the thing is worded. What they were wrong about was the
*conclusion*: they treated "a regulator would want a trial first" and "the data
is thin" as reasons not to compute, in an app whose stated purpose is to derive
insights nobody else will from data people already have. **Thin data is a reason
to print the error bar, not a reason to show nothing.** A permanent null is not
the safe option; it is the useless one.

**The one rule that survives all eleven reversals:** modelled is never dressed as
measured (`MetricSource.calculated`, its own family, weight 0, no reference
range), and every one of these states its own uncertainty. That is what makes
shipping them honest rather than reckless.

- `B5-27` ✅ `w4` `newcards` `hard` `gate:none` `ask` — **Mental health card**
      ✅ Shipped `867129e`.

      **Original reason (kept — it still shapes the wording):** Nine adversarial
      attacks, all do-not-ship. MindfulSession 0 rows, StateOfMind 0, MoodChanges 0;
      the whole symptom log is one row. Every design produced a permanent null, and
      *"you seem fine"* arriving by arithmetic to someone having a bad month is the
      worst available failure

      **✅ Ruling: BUILD IT.** *"I want a mental health card. Figure it out, creative
      licence + data science."* ⚠️ **The one attack that still stands is the failure
      mode, not the feature**: the card must never reassure. It reports what the body
      did, asks what the reader felt, and the arithmetic runs the reader's own answers
      against their own physiology

- `B5-28` ✅ `w3` `bp` `hard` `gate:none` `ask` — **Cuffless blood pressure**
      ✅ Shipped `0ea9411`.

      **Original reason (kept — it still shapes the wording):** Whoop took an FDA
      warning letter; cuffless PPG has no finalised validation protocol. 51 real cuff
      readings are better

      **✅ Ruling: BUILD IT.** Reader: *"Did we not already build the experimental BP
      estimate????"* — **yes, `BloodPressureEstimator` already does exactly this**
      (personal calibration, reports its own ±). The refusal was about *a second,
      cuff-free card*, and reads as a flat no. **What "do it" means here: stop hiding
      it behind the cuff, and give a daily estimate with its error**

- `B5-29` ✅ `w4` `newcards` `hard` `gate:none` `ask` — **Own-brand biological age**
      ✅ Shipped `972e2d7`.

      **Original reason (kept — it still shapes the wording):** You would get a worse
      black box with a smaller *n*. Relaying Oura's with its error attached is strictly
      more honest — that is #26

      **✅ Ruling: BUILD IT.** *"THESE SORTS OF THINGS ARE THE ENTIRE POINT OF THE APP.
      WHY WOULD YOU SAY NO."* Correct, and the refusal misread the brief. The answer to
      "a black box" is **not to build a black box**: every term visible, every weight
      visible, every one attributable

- `B5-30` ✅ `w3` `sleep` `build` `gate:none` `ask` — **Sleep-apnoea card**
      ✅ **SHIPPED as a Sleep section** — breathing disturbance index, trended, never
      asserted.

      **Original reason (kept — it still shapes the wording):** Asserting or screening
      for apnoea is FDA-clearance territory. Trending the index inside Sleep is fine; a
      card whose *name* implies a condition is not

      **✅ Ruling: BUILD IT**

- `B5-31` ✅ `w4` `cycle` `build` `gate:none` `ask` — **Cycle / fertility**
      ✅ Slice 1 `a8be5ae`, slice 2 (phase model, fertile window, phase-aware
      baselines).

      **Original reason (kept — it still shapes the wording):** Zero rows,
      contraceptive claims need clearance, unstated assumption about the reader

      **✅ Ruling: BUILT.** *"Basically do everything Flo does."* Assumption stated
      (Q18: the reader's wife). **The clearance reason survives as the wording rule and
      is now linted**: `CyclePhaseModel.notContraceptionNotice` must be on screen in
      any view drawing a fertile window, and `verify.sh` fails otherwise. Zero rows
      remains the real constraint — on *her* record it is unmeasured, and
      `SyntheticSeed.seededCycleDays` is what makes the tab verifiable on a simulator
      meanwhile

- `B5-32` ❌ `w4` `food` `build` `gate:none` — **Meal-to-outcome / TDEE / intake-driven anything**
      **Original reason (kept — it still shapes the wording):** `dietaryEnergy`: 30
      days ever, **0 in the last 90**. The gate is ~80% of logged days; the reader is
      at 0%

      **❌ Ruling: UPHELD — the only one.** Reader: *"I don't care, don't do it."* The
      single refusal both sides agree on

- `B5-33` ✅ `w3` `newcards` `build` `gate:none` `ask` — **Total sound exposure**
      **Original reason (kept — it still shapes the wording):** Environmental audio
      exists on 14 of the last 90 days — summing it with headphones would invent the
      quiet hours

      **✅ Ruling: BUILD IT.** ◐ **The dose metrics shipped 2026-08-06**:
      `environmentalSoundDose` and `headphoneSoundDose`, each the day's equal-energy
      LEQ in dBA over the hours its own sensor could hear (`SoundDoseModel`, on the
      ingest path, `MetricSource.calculated`, Data tab ▸ Hearing) — never summed into
      one figure, and the denominator is the measured time precisely so no quiet hour
      is ever invented. Nothing scores them yet. ⬜ **Still to come: the card** — #22's
      shape, headphone dose as the number, environmental beside it **with its coverage
      stated**

- `B5-34` ✅ `w3` `newcards` `hard` `gate:none` `ask` — **Physical-effort intensity**
      ✅ **SHIPPED `5ed4ac7`** — `EffortIntensityModel`, WHO substitution, feeds the
      Fitness score (`fitness-v2`).

      **Original reason (kept — it still shapes the wording):** 81,252 rows looks dense
      and is a trap: 13 of the last 90 days. A z-score over a series that exists one
      day in seven is not a z-score

      **✅ Ruling: BUILD IT — as a Fitness section**, which is what the original note
      already recommended. ⚠️ **And the reader added a requirement the refusal never
      considered: the effort score feeds the overall Fitness score.** That is new work,
      not a relocation

- `B5-35` ✅ `w2` `newcards` `build` `gate:none` `ask` — **Steps / distance / flights**
      ✅ **SHIPPED `5ed4ac7`** — three metrics promoted, Fitness sections.

      **Original reason (kept — it still shapes the wording):** Real, but Fitness
      sections rather than a card

      **✅ Ruling: BUILD IT — as Fitness sections.** Reader agrees with the placement

- `B5-36` ✅ `w3` `radar` `hard` `gate:none` `ask` — **Radar accuracy scorecard**
      ✅ Shipped `44fb94e`.

      **Original reason (kept — it still shapes the wording):** An honest sensitivity
      figure is 3–5 years away at one symptom tag. The false-alarm rate is printable
      today and belongs on the radar itself

      **✅ Ruling: BUILD IT**

- `B5-37` ⬜ `w4` `capture` `build` `gate:decision` `ask` — **Daylight/UV, spirometry, mindfulness, mood, oral health, falls**
      **Original reason (kept — it still shapes the wording):** All **zero rows**.
      Data-collection problems wearing a build's clothing

      **✅ Ruling: BUILD THEM.** ⚠️ **The zero-row finding is unchanged and is the
      whole difficulty**: what gets built is the *capture* — a way to put the data in —
      because a reader cannot be shown a chart of nothing. Anything that only reads
      HealthKit here will render empty forever

      ⚠️ The mindfulness and mood halves are gated on a ruling that has not been made:
      roadmap-32 asks for the app's posture toward mental-health data *before* any mood
      or mindfulness capture is built. Zero rows exist for both, so that is a stance
      question rather than a build.

- `B5-38` ✅ `w3` `transparency` `build` `gate:none` `ask` — **"Why is my score low"**
      ✅ Shipped `964c03e` as a section of the deep dive, not a card.

      **Original reason (kept — it still shapes the wording):** Highest-value idea in
      the scan, and it must not be a card — an explanation one tap from the number is
      one nobody reads

      **✅ Ruling: Reader agrees it is not a card, and placed it:** *"I want this to be
      part of the deep dive under the insight web."* More specific than §C's "under
      every score" — see S2

---

## B6 — The calendar brief (reader, 2026-08-06)

**A whole feature in one message, and it is bigger than the integration it
extends.** Recorded in full because it is the specification:

> *"When we import the calendar data, I want it to use AI to read the meetings
> and their content, and actually rank each calendar item on: was this work or
> personal? Was this actually a meeting, or just something like a reminder? Did
> it have a location (meaning I had to be somewhere), or did it include a remote
> meeting link? How long was the meeting? Was it a marathon workshop? Was it
> travel? … The sentiment of the meeting — is it a chill catchup or a formal
> meeting with a client?*
>
> *And to see this, I want a section in both cards that shows the list of items
> from your calendar, and the relevant details for each item, with an
> opportunity to correct them or confirm, which the model can learn from … These
> will then go into the overall score of the card.*
>
> *And uniquely, this will become a new data source, like Work Events, Personal
> Events, Travel Events."*

- `C1` ✅ `w3` `calendar` `build` `gate:none` `ask` — **The six axes as a typed model**
      `CalendarEventClassification`, with `Decider` per axis (fact / rules /
      model / reader).

      Shipped 2026-08-06.

- `C2` ✅ `w3` `calendar` `build` `gate:none` `ask` — **The deterministic classifier**
      `CalendarEventClassifier`, 23 tests. Answers duration, presence, travel,
      reminder-vs-meeting exactly; leaves work-vs-personal on an ambiguous
      title, and sentiment, for the model.

      Shipped 2026-08-06.

- `C3` ✅ `w3` `calendar` `hard` `gate:none` `ask` — **`loadHours`**
      An hour of formal client meeting in a room is not an hour of blocked
      focus time, and a reminder costs nothing. This is what a card's score
      reads.

      Shipped 2026-08-06. `CalendarEventClassification.loadHours` is at
      `InsightKit/Sources/InsightKit/Models/CalendarEventClassification.swift:192`.

- `C4` ✅ `w3` `calendar` `build` `gate:none` `ask` — **The learning loop's data shape**
      `CalendarEventJudgement` keeps the guess and the reader's correction
      **apart**, so accuracy is measurable and re-classifying cannot overwrite
      the reader. `CalendarClassifierAccuracy` refuses a figure below 10
      reviews.

      Shipped 2026-08-06.

- `C5` ✅ `w3` `calendar` `build` `gate:none` `ask` — **The buckets**
      `CalendarEventBucket`: Work / Personal / Travel / Other, derived not
      stored, travel outranking the calendar it was booked in.

      Shipped 2026-08-06.

- `C6` ✅ `w3` `calendar` `build` `gate:none` `ask` — **The on-device model call**
      `FoundationModelSummarizer` shows the pattern
      (`SystemLanguageModel.availability`, `LanguageModelSession`, graceful
      fallback). It may only move **context** and **formality** —
      `CalendarEventClassifier.refined` enforces that and is tested.
      ⚠️ Prompt must never leave the device.

      Shipped 2026-08-06.

- `C7` ✅ `w3` `calendar` `build` `gate:none` `ask` — **Persisting events and judgements**
      Nothing survives a launch today. A `@Model` per event and per judgement,
      registered in `DataStore`'s schema (an unregistered `@Model` silently
      never persists).

      Shipped 2026-08-06. ⚠️ The "nothing survives a launch today" sentence is
      the *original* reason and is kept because it still shapes the wording —
      it has been false since `77847b2`. `CalendarEventRecord`
      (`PersistenceModels.swift:232`) and `CalendarJudgementRecord` (`:349`)
      are both `@Model`s, both registered in `DataStore.swift:21`'s `Schema`,
      written by `DataStore.mergeCalendarEvents` (`:485`) from
      `AppModel.syncCalendar` (`:350`), read back by `loadCalendarEvents`
      (`:473`) into `AppModel.calendarEvents` (`:249`), and bound into the
      engine at `InsightEngine.swift:89-93`. Any row elsewhere in the docs
      calling calendar persistence a blocker is describing a closed item.

- `C8` ✅ `w3` `calendar` `build` `gate:none` `ask` — **The review section, in both cards**
      The list with its details and confirm/correct controls.

      Shipped 2026-08-06. Its behaviour was then rebuilt by §B8's R1/R2
      (`5ecb246`) — see that section before touching it.

- `C9` ✅ `w3` `calendar` `build` `gate:none` `ask` — **The buckets as `DataDomain` cases**
      Work Events, Personal Events, Travel Events in the Data tab.

      Shipped 2026-08-06.

- `C10` ✅ `w3` `calendar` `build` `gate:none` `ask` — **Travel drain (#15) and work impact (#16) themselves**
      Shipped 2026-08-06.

      ⚠️ Any row still saying these need the events persisted first is stale.
      `TravelDrainInsight` and `TravelDrainModel` exist and read the persisted
      events (`CalendarInsights.swift:859`, `:620`); work impact also shipped
      its `work-impact-v2` scoring (`ee14ad1`). Only real-data verification on
      the reader's phone remains for either — which is C11.

- `C11` ⬜ `w1` `calendar` `build` `gate:phone` — **Nothing has been seen with real calendar data in it**
      ✅ **All ten rows shipped 2026-08-06.** What remains is not in this
      table: **nothing has been seen with real calendar data in it.** Every
      card and section was verified in its *empty* state on the simulator,
      which is the state that matters most (two cards shipped invisible on
      2026-08-03) — but the populated state, the classifier's accuracy on real
      titles, and whether the on-device model is even available on the
      reader's phone are all unverified. ⚠️ **The reader's own export has no
      calendar in it at all**, so this needs the phone.

## B7 — Calendar identity & holidays (reader, 2026-08-06, second brief)

**The reader's own words, recorded in full because they are the specification:**

> *"on the work and travel events, i need to be able to handle cases such as
> someone just putting an 'OOO' or 'out of office' block in my calendar,
> sometimes its mine, sometimes its not. Also this just made me realised.. I
> should be able to see holidays (e.g. my calendar blocks that are holidays).
> It should also be email aware, and user aware.. maybe I can input my name to
> give context to the events (e.g. did i organise the meeting, or did i just
> attend) and if it is something like a 'John smith on holiday - OOO' It can
> see if that is me, or someone else. I could also be asked input my emails,
> as input data, like my work email … and personal emails …, which will again
> give even more context to those meetings and travel. I should also be able
> to input holidays that are planned manually.. e.g. in the work impact, the
> travel impact, the stress, the mental health.. knowing you have, or have not
> been on a holiday is a very good data point (we should of course make sure
> it has a data tab, where I can track holidays). And one recommendation could
> be, if i'm stressed, my health is bad.. maybe I should be recommended to
> take some leave, take a long weekend.. and it could even look at my calendar
> and predict when is a good time since it knows my calendar!!"*

**Sequencing:** H1 unblocks H2; H3+H4 unblock H5; H5 unblocks H6; H7 reads H5
and H6's inputs — now carried as `gate:needs:` on each row rather than as
prose. H1–H4 were one session's work and landed in `bc9c6e3`; H6 touches four
scoring models and their versions; H7 is small once H5 exists. ⚠️ **Identity
data (name, emails) is personal data in a public repo's docs — record the
*shape* (`work email`, `personal email`), never the reader's actual addresses,
per `docs/privacy-and-ip.md`.**

- `H1` ✅ `w3` `calendar` `build` `gate:none` `ask` — **Identity inputs**
      The reader's name, work email(s), personal email(s), as `InputKind`s
      (⚠️ load `add-data-or-input`: four surfaces, three checks). What they
      buy: organiser-vs-attendee on every event, and whose OOO a block is.

      Shipped `bc9c6e3`. `ReaderIdentity`
      (`InsightKit/Sources/InsightKit/Models/ReaderIdentity.swift:46`) holds
      name, work and personal emails, with word-boundary name matching and
      mailto-tolerant organiser matching. Stored as a JSON file beside the
      profile, read by the classifier and by nothing else; **the export has no
      key for it**, deliberately. Its own `InputKind` (`.readerIdentity`, one
      sheet for the whole conversation — `ReaderIdentitySheet`,
      `HealthInsights/Features/Inputs/AddDataView.swift:729`), offered on the
      Work impact card via `ContributionRoute.readerIdentity` (`.offeredOnly`
      — the card its OOO handling moves; travel drain reads no classifications
      and deliberately offers nothing).

- `H2` ◐ `w3` `calendar` `build` `gate:needs:H1` `ask` — **OOO / out-of-office handling**
      An "OOO" block is *someone's absence*, not a meeting. Whose it is
      decides everything: **mine** → a holiday/leave signal; **someone else's**
      → near-zero load, possibly *reduced* load (fewer meetings that week).
      Needs H1 to answer "mine?" — without identity, an OOO block must
      classify as ambiguous, never as work.

      Mostly shipped `bc9c6e3`; H1 landed in the same commit, so the gate above
      is already satisfied. Two new `Occasion` outcomes
      (`CalendarEventClassification.swift:87`, `:96`): an absence marker that
      names the reader, or that they organised, is `.leave` ("Your leave");
      someone else's — or any absence with no identity to resolve it — is
      `.absence` ("Someone away"), and neither is ever a meeting, ever load, or
      ever the work bucket. "Holiday party" stays a party (eventful-word veto),
      "Leave for airport" stays travel, and the absence check runs before the
      multi-day travel rule so a multi-day "Annual leave" is leave.
      `CalendarEvent` gains `organizerIsReader` — the fact, never the address,
      the `hasVideoLink` pattern — derived in `CalendarIntegration` from
      `isCurrentUser` and the stated emails. Entering identity later re-reads
      only the occasion of stored guesses (`reoccasioned`), preserving
      model-decided axes and every correction.

      ⚠️ **What is not built, and is the reason this is ◐ rather than ✅:** the
      brief's *"possibly reduced load"* half. `loadHours` returns `0` for both
      `.leave` and `.absence` (`CalendarEventClassification.swift:200`) — a
      colleague's OOO contributes nothing, but it never *reduces* the week's
      load below what the remaining meetings imply. The brief hedged that with
      "possibly", so this may be a deliberate no; it has not been ruled on
      either way.

- `H3` ✅ `w3` `calendar` `build` `gate:none` `ask` — **Holiday detection from the calendar**
      All-day/multi-day blocks reading as leave ("annual leave", "holiday",
      "vacation", "PTO", "OOO" when mine). A new classification outcome, not a
      new bucket bolted on: it feeds H5's ledger.

      Shipped `bc9c6e3`. All-day and multi-day blocks classified as the
      reader's own leave become detected holidays — **dates only, titles never
      travel**.

- `H4` ✅ `w3` `calendar` `build` `gate:none` `ask` — **Manual holiday input**
      Planned leave entered by hand, past and future. Its own `InputKind` + a
      **Holidays `DataDomain`** (reader: *"make sure it has a data tab, where I
      can track holidays"*).

      Shipped `bc9c6e3`. `HolidayEntry` (`@Model`, registered in the schema —
      `PersistenceModels.swift:308`) holds hand-entered leave, past or planned,
      with its own `InputKind` (`.holiday`, `HolidayEntrySheet` at
      `AddDataView.swift:856`). ⚠️ `.holiday` is **`settingsOnly` until H6** —
      it appears in Settings ▸ Add or update data but is deliberately kept off
      the Today `+` menu until a card reads the ledger. `DataDomain.holidays`
      gives it the Data tab page the reader asked for by name
      (`DomainDataScaffold`, delete on entered rows only —
      `DomainDataViews.swift:225`).

- `H5` ✅ `w3` `calendar` `build` `gate:needs:H3,H4` `ask` — **One holiday ledger, two sources**
      Detected (H3) and entered (H4) merge into one dated record of leave,
      deduplicated, correctable. This is the data point the cards read.

      Shipped `bc9c6e3`; H3 and H4 landed in the same commit, so the gate above
      is already satisfied. `HolidayLedger`
      (`InsightKit/Sources/InsightKit/Models/HolidayLedger.swift:39`) merges
      detected and entered into one deduplicated record — overlaps collapse,
      the reader's label and source win, adjacency stays separate — with
      `daysSinceLastLeave` (0 on leave, `nil` when only booked ahead) and
      `leave(in:)`. The export gains a real `holidays` key (schema v3) — **the
      one calendar-derived thing that can export, because it carries no
      words.**

- `H6` ⬜ `w3` `calendar` `hard` `gate:needs:H5` `ask` — **Cards read the ledger**
      Work impact, travel drain, stress load, mental health each get "you have
      / have not had leave recently" as an input. ⚠️ Each card that scores it
      needs its `modelVersion` bumped, per the fitness-v2 precedent.

      ⚠️ Not started, and **deliberately** so: `bc9c6e3` left H6 and H7 unwired
      because each scoring model that takes leave needs its `modelVersion`
      bumped. H5 has shipped, so the gate above is satisfied and this is the
      next thing in the brief to pick up. Nothing outside the Data tab reads
      `HolidayLedger` today — `AppModel.holidayLedger` (`AppModel.swift:319`)
      builds it, `DataTabView.holidayStanding` (`:468`) and the holidays domain
      page read it, and no `InsightModel` does. `InputKind.swift:242` records
      the same thing from the other side. Wiring it is also what promotes
      `.holiday` off `settingsOnly` (H4).

- `H7` ⬜ `w3` `calendar` `build` `gate:needs:H5,H6` `ask` — **The leave recommendation**
      "Stressed + degraded health + no leave in N months → suggest a long
      weekend", and **predict a good window from the calendar** (quiet weeks,
      no marathon days, adjacent to weekends/public holidays). Goes through
      `Suggestions`/`SuggestionEngine`, ranked below grounding gaps like
      everything else.

      Not started. Small once H5 exists — H5 has shipped, so the real block is
      H6.

---

## B8 — The review loop, the correction record, and two-tier sharing (reader, 2026-08-06)

**The reader's words, recorded in full because they are the specification:**

> *"In travel events and work events, when i go to confirm, i cannot un-confirm
> in case i did want to actually change it. The same as not correct, if I just
> select one correction metric (e.g. change from work to personal) i cannot
> correct any of the other metrics, it just disappears.. so let me change as
> many as needed, then confirm before i say 'corrected', and when i do confirm
> it should stay in the list, not disappear!*
>
> *In terms of data, make sure for model improvement, we remember the 'AI
> Estimated' and 'Corrected' data points, in addition to the whole
> email/artifact, so when we feed this back for improvement, it has all the
> context to improve the model. all of this needs to be in the export, and in
> the improvement section we created.*
>
> *I understand this will contradict the 'stuff never leaves your device', maybe
> we update that to read something like a 2-tier opt out (both opted in by
> default). E.g. Full - is share both Full Data (e.g. FUll Calendar plus before
> and after categories, Meta Data only - sharing just share the correction
> metadata (e.g. Blood pressure esitmate is 13 BP above actual cuff)"*

- `R1` ✅ `w3` `calendar` `build` `gate:none` `ask` — **The review row stops being one-shot**
      Today choosing one axis commits and dismisses. It must let the reader
      change **as many axes as they want** — context, formality, whether it was
      a meeting, travel, presence — and only then confirm. Nothing leaves the
      list on a single tap.

      ✅ **SHIPPED** — draft layer, every axis editable, Change un-confirms
      (`5ecb246`).

- `R2` ✅ `w3` `calendar` `build` `gate:none` `ask` — **Confirm is reversible, and confirmed rows stay**
      An un-confirm, and a confirmed row remains visible with its state shown
      rather than vanishing. ⚠️ The reader's own emphasis: *"when i do confirm
      it should stay in the list, not disappear!"*

      ✅ **SHIPPED** — draft layer, every axis editable, Change un-confirms
      (`5ecb246`).

- `R3` ✅ `w3` `calendar` `build` `gate:none` `ask` — **The correction record keeps all three layers**
      What the model guessed, what the reader corrected it to, **and the
      artifact it judged** (title, location, attendees, duration — the whole
      event). C4 already keeps guess and correction apart, which is the hard
      half; the artifact snapshot is what is missing, and it is what makes a
      correction *trainable* rather than merely counted.

      ✅ **SHIPPED** `ce9f76f`.

- `R4` ✅ `w3` `export` `build` `gate:none` `ask` — **All three reach the export and the improvement section**
      ⬜ Open. R3 shipped the three-layer record itself (`ce9f76f`), so this is
      unblocked: the guess, the correction and the artifact snapshot all have
      to appear in the export and in Settings ▸ Data & model improvement, per
      the reader's *"all of this needs to be in the export, and in the
      improvement section we created."*

      ⚠️ It carries the whole event's words — titles, locations, attendees —
      so what it emits is governed by R5's tier and by the closing warning
      below, not by the export's usual rules. H5's holidays key is the
      contrast: it exports because it carries no words.

- `R5` ✅ `w3` `transparency` `build` `gate:none` `ask` — **Two-tier sharing replaces the binary toggle**
      Both tiers on by default, at the reader's explicit instruction.
      **Full** — the artifact plus the before/after categories.
      **Metadata only** — the correction shape without the content ("the BP
      estimate ran 13 mmHg above the cuff", "a work guess was corrected to
      personal").

      ✅ **SHIPPED** `ce9f76f`.

- `R6` ✅ `w3` `transparency` `build` `gate:none` `ask` — **Every claim that nothing leaves the device is rewritten to be true**
      Settings ▸ Data & model improvement, `TelemetryEvent`'s doc comment,
      `DiagnosticsLog`'s class comment, the export's own copy. ⚠️ **A stale
      privacy promise is worse than no promise** — it is the one kind of copy a
      reader is entitled to rely on.

      ✅ **SHIPPED** `ce9f76f`.

⚠️ **The one thing to revisit before a second user exists.** Defaulting *Full*
to on means full calendar text — titles, locations, attendees, the most
identifying strings this app holds — is shared unless turned off. For one
person who owns the app and the server, that is the reader's call and standing
rule 0 covers it. **At rollout it is not**: consent for content of this kind is
opt-in in most jurisdictions, and `docs/privacy-and-ip.md` already records that
§B5's original reasons return the moment there is a second user. Recorded here
so the decision is deliberate rather than inherited. See
`docs/norms-and-telemetry.md` for what may be pooled and what may never be.

---

## C. Sections requested

- `S1` ✅ `w3` `chrome` `build` `gate:none` `ask` — **A bespoke section on EVERY card**
      Applies to **All** cards. Shipped `adca807`. `bespokeSection` is
      exhaustive now, so `default: EmptyView()` is gone and a new card cannot
      ship without a stated decision. Readiness is the one deliberate
      `EmptyView`, because its picture (the seventeen-vital strip) is drawn
      universally.

      Original note, kept because it still shapes the wording and records the
      reader's instruction: ⚠️ **Five have none today: gait, sustainedLoad,
      nutrition, metabolism, readiness.** `InsightDetailView` has
      `default: EmptyView()` with a comment arguing *against* exhaustiveness —
      **your instruction reverses that.** Make the switch exhaustive so a new
      card cannot ship without one, and **update `docs/card-sections.md` and
      the `add-insight` skill** so it is enforced, not remembered. For gait it
      is the worst case: the speed = step length × cadence decomposition is the
      card's reason to exist and reaches you as one driver line.

      ⚠️ Verified against the code 2026-08-07: that note is stale in every
      particular. `InsightDetailView.swift:79-160` is exhaustive over all 26
      `InsightID` cases and renders `gaitDecompositionCard`,
      `sustainedLoadChannelsCard`, `micronutrientCard` and
      `energyBalanceCard`. Readiness is the single deliberate `EmptyView` with
      a documented reason. **Four rows described this one already-closed
      item** — S1 here, N3 in §0, and D18 and D1 in §E. Close all four
      together; do not schedule any of them as work.

- `S2` ✅ `w3` `transparency` `hard` `gate:none` `ask` — **Score decomposition**
      Shipped `964c03e`. Each signal, its value, its baseline, its deviation,
      its weight, and the counterfactual.

      ⚠️ **In the deep dive under the insight web** — the reader placed it
      there explicitly on 2026-08-06, in preference to "under every score".

      Item #38 above. Oura's #1 unfixable complaint.

- `S3` ✅ `w3` `radar` `build` `gate:none` — **"Nights to flag" detail sheet — slides up, slides back down**
      Symptom radar. 1.0 SD → 14 nights · 1.5 → 6 · 2.0 → 4 · 3.0 → 3.

- `S4` ✅ `w3` `radar` `build` `gate:none` `ask` — **Flagged days over time — when sickness was flagged and how it builds**
      Symptom radar. Your idea tonight.

- `S5` ✅ `w3` `sleep` `build` `gate:none` `ask` — **"What changed while you slept"**
      Sleep. Your request.

- `S6` ✅ `w3` `newcards` `build` `gate:none` `ask` — **Recovery tracker**
      Readiness. Your request.

- `S7` ✅ `w3` `substances` `build` `gate:none` — **SubstanceEpisodes explained**
      Substance Impact. The occasion is the unit of evidence; three drinks in
      one evening is one exposure, a fortnight later is another; substances
      never merge.

      The logic exists and is tested; **nothing shows it to you**.

- `S8` ✅ `w3` `devices` `build` `gate:needs:B3-23` `ask` — **Device disagreement — pick a signal, see the difference and why**
      Card or section — see #23.

      ⚠️ Unsettled: §C records it as a section, §B3 #23 records it as a card,
      and the reader has asked for it again without settling which. That
      ruling comes first.

      ✅ **RULED 2026-08-07 — same item as `B3-23`, and the answer is both.**
      This row is the *section* half; `B3-23` is the index card. Build the
      sections here, then the card that indexes them.

- `S9` ✅ `w3` `sleep` `build` `gate:none` — **Breathing disturbance, charted not scored**
      Sleep. 107 days, only 10% redundant. Oura publishes no validated curve,
      so it must not be scored.

- `S10` ✅ `w3` `sleep` `build` `gate:none` — **Overnight HRV**
      Sleep is the only card grading a night that reads nothing from the
      heartbeat stream recorded during it.

- `S11` ✅ `w3` `sleep` `hard` `gate:none` — **Sleep Regularity Index replacing two crude estimators**
      Sleep. Beat duration head-to-head for mortality in UK Biobank
      (n=88,975). **Net weight change zero**.

- `S12` ✅ `w3` `charts` `build` `gate:none` — **Intensity distribution**
      Fitness. Where PhysicalEffort legitimately belongs (#34).

- `S13` ⬜ `w3` `sleep` `build` `gate:needs:S9` — **Trend the breathing-disturbance index**
      Sleep. The describable half of #30.

---

## D. Integrations

- `I1` ✅ `w3` `calendar` `build` `gate:none` — **Calendar — sync one or many Apple calendars, and unsync, under Integrations like the others**
      ✅ **Shipped `pending`**: `CalendarIntegration` in the registry,
      permission prompt, connect/disconnect, per-calendar selection persisted,
      and `CalendarEvent`/`CalendarModel` in InsightKit with committed hours,
      time-zone changes and busiest day — all testable on Linux.

      Original warning, kept because it is what #15 and #16 were scheduled
      against: ⚠️ **Events are fetched but not yet STORED**: nothing persists
      them between launches and no card reads them. That is the remaining
      half, and it is what #15 and #16 need.

      ⚠️ Verified against the code 2026-08-07: that warning has been false
      since `77847b2`. `CalendarEventRecord` (`PersistenceModels.swift:232`)
      and `CalendarJudgementRecord` (`:349`) are both `@Model`s, both
      registered in `DataStore.swift:21`'s `Schema`, written by
      `DataStore.mergeCalendarEvents` (`:485`) from `AppModel.syncCalendar`
      (`:350`), read back by `loadCalendarEvents` (`:473`) into
      `AppModel.calendarEvents` (`:249`), and bound into the engine at
      `InsightEngine.swift:89-93`. The persistence half is done, so **N2 is
      not a blocker for anything**, and #15 (Travel drain) and #16 (Work
      impact) are both built against the persisted events — only real-data
      verification on the reader's phone remains for either.

- `I2` ⬜ `w3` `integrations` `build` `gate:external` — **Hume Band direct API**
      Flows in via Apple Health only.

- `I3` ⬜ `w3` `integrations` `build` `gate:external` — **Ultrahuman**
      Nothing.

- `I4` ⬜ `w3` `integrations` `build` `gate:external` — **Garmin**
      Nothing.

- `I5` ⬜ `w3` `integrations` `build` `gate:external` — **Fitbit**
      Nothing.

- `I6` ✅ `w4` `labs` `hard` `gate:none` — **Arbitrary lab analytes via Foundation Models**
      Only known analytes parse today.

- `I7` ✅ `w4` `capture` `build` `gate:none` — **ECG photo/PDF import with metadata**
      Nothing.

- `I8` ⬜ `w4` `food` `build` `gate:phone` — **Barcode scanner with on-device lookup (Open Food Facts / USDA)**
      Nothing. ⚠️ Photo-to-number is out: real-world portion estimation runs
      as low as 39% accurate. The flow must be photo → candidates → you
      confirm → portion.

- `I9` ⬜ `w4` `bodyscan` `build` `gate:phone` — **Camera + LiDAR guided body scan**
      Mesh renders; **no capture exists**.

---

## E. Defects and quality gaps

- `D1` ✅ `w1` `newcards` `build` `gate:none` — **Five cards have no bespoke section**
      Cross-referenced to §C `S1`.

      ⚠️ Fixed (`adca807`) — and it was never five. `InsightDetailView.swift:79-160`
      is exhaustive over all 26 `InsightID` cases and renders
      `gaitDecompositionCard`, `sustainedLoadChannelsCard`, `micronutrientCard`
      and `energyBalanceCard`. Readiness is the single deliberate `EmptyView`,
      with a documented reason — its picture is the seventeen-vital strip, drawn
      universally. **This row and D18 are the same item written twice**, and §0's
      N3 and §C's S1 are the same item a third and fourth time. Four rows
      describing one already-closed thing.

- `D2` ✅ `w1` `nutrition` `build` `gate:none` — **`MicronutrientTargets` is dead code with a user-visible cost**
      Cross-referenced to §A `Q4`.

      ⚠️ Fixed. `MicronutrientTargets` is referenced in three files now, per §A Q4,
      shipped `5330d92`.

- `D3` ✅ `w1` `chrome` `build` `gate:none` — **Feedback gated on `primaryValue != nil`**
      Cross-referenced to §A `Q5`. Original reason kept because it still shapes the
      wording: the cards most in need of correction were the ones that could not be
      corrected.

      ⚠️ Fixed. `InsightDetailView.swift:3669-3683` now branches the *wording* on
      `primaryValue == nil` rather than hiding the affordance — that is §A Q5's
      "Ungate", shipped `5330d92`.

- `D4` ✅ `w3` `testdebt` `build` `gate:none` — **Five dead modules**
      `PostureAssessment`, `MedicationScanner`, `BodyLimb`, `RingProvenance`,
      `BodyMeshConfiguration` — each appears in exactly one file, its own
      declaration.

      ⚠️ This row said **six** and named `MicronutrientTargets` first. That one is
      live now (three referencing files, §A Q4, `5330d92`), so the count is five.
      The remaining five split two ways: `BodyLimb` and `BodyMeshConfiguration`
      belong to the unbuilt body-scan capture surface, the other three to nothing
      currently planned.

- `D5` ✅ `w0` `testdebt` `build` `gate:none` — **The app target has ZERO tests**
      Every render site, every visibility gate, every hot path.

      ⚠️ The zero-tests claim is exactly right — `project.yml` declares one target
      and the pbxproj contains no unit-test product type — but the supporting
      figures in this row were stale. The app target is **28,573 lines, ~39%** of
      shipped Swift, not "23,813 lines, roughly 41%".

      ⚠️ And closing it is not "write a test file": it needs a **new `project.yml`
      target plus an `xcodegen` regeneration**, and a hosted Linux session can
      never gate it — there is no iOS SDK there, so CI or a Mac session is the
      only place the new target can be proven.

- `D6` ✅ `w3` `chrome` `build` `gate:none` — **No accessibility work**
      23 modifiers total; charts, radar web and body mesh carry no VoiceOver
      description; no Dynamic Type.

- `D7` ◐ `w4` `chrome` `build` `gate:none` — **English only**
      All copy hard-coded in Swift literals; imperial units unchecked.

- `D8` ◐ `w4` `chrome` `build` `gate:none` — **No widgets, Live Activities or watch target**
      For a product whose central object is a daily number.

- `D9` ⬜ `w2` `pipeline` `mech` `gate:phone` — **Cold-launch time against the real 320,913-row record is unmeasured**
      Launch screen may be covering 1 second or 15.

- `D10` ✅ `w2` `integrations` `build` `gate:none` — **No unhappy path walked**
      What you see when an Oura/Withings token expires mid-sync, or HealthKit is
      partially denied, is unknown.


      ◐ **Part done 2026-08-07. What the agent left, in its own words:**
      Two of the four paths were walked on the simulator and are now known; tw
      o could not be. WALKED: (1) HealthKit denied — tapped "Don't Allow" on t
      he real Health Access sheet, and the app showed a green tick and the wor
      d "Connected" in onboarding (OnboardingView.swift:98-101), then "Synced 
      22 seconds ago" in green in Settings (SettingsView.swift IntegrationRow)
      , while Today told the reader "Connect a wearable", "Wear your watch to 
      sleep" and "Connect a sleep source" — four instructions for things they 
      had already done. requestAuthorization() succeeds whether the reader tap
      s Allow or Don't Allow, so nothing anywhere knew. (2) A HealthKit connec
      t that throws — the raw NSError string is printed verbatim in the Settin
      gs row (AppleHealthProvider.swift:67 `status = .error(error.localizedDes
      cription)`); I saw "Missing com.apple.developer.healthkit entitlement." 
      sitting in the reader's Settings screen. NOT WALKED: a provider token ex
      piring mid-sync, a connector that stopped syncing silently, and no netwo
      rk. All three need real Oura/Withings credentials and a live account; th
      e simulator has none and there is no injection point (tokens live in the
       Keychain behind ProviderCredentialStore). I established all three from 
      the code with certainty rather than inference — OuraProvider.fetchData n
      ever throws (it records per-collection failures at OuraProvider.swift:16
      0-172 and returns), OAuthIntegration.sync() had no catch, and Integratio
      nRegistry.syncAllConnected used `try?` — and fixed the class at the sour
      ce, but I have not SEEN them. HealthKit *partial* denial specifically al
      so cannot be distinguished on the simulator: it has no Health app and no
       data, so a partially-denied phone and a fully-denied one render identic
      ally there. The case the row calls the worst failure this app has — a ca
      rd reading 100 because its instrument is absent — still needs the phone.

- `D11` ✅ `w2` `data` `mech` `gate:none` — **The nine Data-domain detail pages have never been opened and checked**
      Against the four promises `docs/data-conventions.md` makes.

- `D12` ✅ `w2` `chrome` `mech` `gate:none` — **Onboarding never checked**
      Including whether the two now-mandatory grounding asks can be skipped.

- `D13` ✅ `w3` `capture` `build` `gate:none` — **The document/OCR path is unaudited**
      The one input that turns a photograph into a health number.

- `D14` ✅ `w0` `rules` `mech` `gate:none` — **Roadmap rows 33–38 are stale**
      The symptom-radar rows all shipped.

- `D15` ✅ `w0` `rules` `mech` `gate:none` — **`docs/card-sections.md` partially corrected 2026-08-06**
      Two false "closed" claims reopened; the per-section tables still need a sweep.

- `D20` ✅ `w3` `devices` `hard` `gate:none` — **A second vendor age is already arriving and nothing reads it: Withings "metabolic age"**
      ⚠️ `withings.measure.227`, 153 rows over 110 distinct days, 2024-12-25 →
      2026-07-29 — a *longer* history than Oura's vascular age. Raw-only: no
      `MetricType`, no promotion rule.

      ⚠️ **And the 227 → "Metabolic age" mapping is an inference** —
      `RawFieldPresentation` named it by matching the reader's own value range
      against plausible meanings, because Withings publishes no public table.
      Printing "Withings says you are N" on that basis is a stronger claim than
      this app has ever made from an inference. **Decide before coding**: relay it
      labelled as an inference, or leave it raw.

      Found by the age scout, 2026-08-06.

      ✅ **RULED 2026-08-07: relay it, labelled as inferred.** Show the Withings
      figure in the age comparison **with the field-mapping uncertainty stated on
      the row itself** — that we believe `withings.measure.227` is their
      metabolic age, and that Withings publishes no field table to confirm it.

      Consistent with relaying Oura's vascular age, and the section's whole point
      is naming who computed what. ⚠️ **The label is not optional and is not
      boilerplate**: this is the first number in the app whose *identity* is
      inferred rather than its value modelled, so the row states a different kind
      of uncertainty from every other row beside it. Do not let it inherit the
      standard "modelled" caveat and call that done.

- `D21` ✅ `w1` `newcards` `hard` `gate:none` — **Fitness age and heart age still collapse multiple instruments**
      Both read one source through `VitalReader.reading`; the reader's export has 4
      VO₂max source ids and 4 systolic ones. The vascular row was fixed on
      2026-08-06 but "all the sources" is only two-thirds true.

      Same scout.

      **Closed 2026-08-07.** `AgeComparison.fitnessEstimates` and
      `.heartEstimates` build one row per instrument off
      `MultiSource.breakdown`, and every row names the instrument in its
      attribution even when there is only one. Two things were decided while
      doing it and are worth carrying:

      1. **Each row is still read through `VitalReader`, one source's series at
         a time** — not off the newest raw sample the way the *vendor* rows are.
         That is what makes the winning row reproduce the Fitness card's and the
         risk card's headline exactly. Reading the raw sample would have traded
         one silent disagreement (between instruments) for another (between this
         section and the card above it).
      2. **The reader's own cuff needed no special case.**
         `DataStore.saveGrounding` already mirrors every entered cuff reading
         into a `.bloodPressureSystolic` sample under `MetricSource.manual`, so
         the breakdown carries it as its own series. A hand-written "the reading
         you entered" row would have printed it twice — two instruments agreeing
         perfectly, on a section about disagreement.

      `HeartAgeAnalyser.Analysis` gained `subject` so the section can re-solve
      SCORE2/ASCVD per blood-pressure source; only the systolic varies between
      those rows, which is what makes their spread attributable to the
      instruments rather than to the person.

- `D22` ✅ `w1` `charts` `build` `gate:none` — **No age series but heart and fitness**
      `AgePoint` has exactly two optional age fields, so neither vascular age nor
      the app's own biological age can be drawn over time — the time chart and the
      comparison section disagree about how many ages exist.

      Same scout.

      **Closed 2026-08-07.** `AgePoint` carries four ages; `HeartAgeHistory.replay`
      fills `vascular` from the analysis field it had been computing and throwing
      away since it shipped, and `biological` from `BiologicalAgeModel`.
      `AgeHistoryChart` draws them from a `Series` table rather than four
      hand-written blocks, and the **Biological age card gained "Your ages over
      time"** above the comparison section — without that it would still have
      been a model with no history, which is the half its own documentation says
      matters (the level is ±10 years; the direction survives).

      Three decisions worth keeping:

      - **The replay's guard was wrong for the new series.** It skipped any day
        without a heart *or* fitness age, so a reader with only a ring — no cuff,
        no outdoor runs — would have got an empty chart from the change that was
        meant to give them one. It is `leadingAge != nil` now.
      - **The biological age is replayed monthly** (`biologicalStrideDays`), not
        weekly. Its markers are read over 90-to-365-day windows, so consecutive
        weekly solves share 51/52nds of their input — thirteen real points and
        thirty-nine restatements. It is also the most expensive model in the app
        and the only one here that scans HRV and walking speed. Both facts point
        the same way. The newest point always carries one, so the chart's last
        value and the card's own number describe the same day.
      - **Exactly one filled band, whatever is drawn** (`AgeHistoryChart.banded`)
        — `add-chart` §8. Translucent bands mix into a third colour that means
        nothing; that was tolerable while only one could ever be drawn and stops
        being theoretical with four series available. The card's own age keeps
        the band; anything beside it is a line.

      ⚠️ **Still open, and it belongs to whoever draws these next:** the replay
      carries a value forward from the last day an instrument reported, so a
      vendor that went quiet draws a flat *measured-looking* line rather than
      stopping. That is pre-existing — the heart and fitness lines have always
      done it, and `SectionCaveat.replayedHistory` is the only thing saying so —
      but `add-chart` §3 says a stretch nobody measured must not be drawn solid.
      Worth its own row — `D50`.

- `D19` ✅ `w0` `rules` `mech` `gate:none` — **A hard-coded count inside reader-facing copy**
      ⚠️ A section said "All four" on a card running on three signals, and mental
      health named all four behaviours including one it had no data for. The
      ledger's "hard-coded count going stale" row, except in a sentence, where it
      is worse: the sentence is a claim about what was looked at. Fixed in
      `adca807` with a test; **no lint exists for the next one**.

- `D17` ✅ `w1` `pipeline` `mech` `gate:phone` — **`walkingSpeed` reads 0 days in the last 90 in the simulator**
      ⚠️ Contradicting the 1,093-day figure measured against the raw export
      catalogue. The Gait card still scores 100, on its other two channels, with
      the speed channel silently absent.

      ⚠️ This row previously offered two causes and **both are wrong**. The
      promotion path is correct — `HealthKitService.swift:175-178` maps all three
      gait identifiers, commit `3853446`, 2026-08-05 — and
      `scripts/load-real-export.sh` is a verbatim copy by design (`:19-26`).

      The real cause is dates: the seed export
      `~/HealthSeed/exports/health-insights-export.json` was written **2026-08-04,
      one day before** the gait promotion commit, so all **27,248** walking-speed
      rows sit in `unmodelled` and can never appear in `samples`. Nothing in the
      app can fix this — it closes when the reader takes a fresh export from the
      phone.

      ✅ **Resolved 2026-08-07 by the reader's new export, and the diagnosis
      holds.** Loading `health-insights-export-new.json` into the simulator and
      reading the app's own import log: **`ok: 16931 × Walking Speed`**, plus
      16,930 step length and 15,282 double support. The gait triad promotes
      correctly.

      So neither of the two causes this row originally named was right — the
      promotion path was never broken. The real cause was that the **old** seed
      export was written 2026-08-04, one day *before* the gait promotion commit
      `3853446`, so its rows sat in `unmodelled` and could never reach `samples`
      however many times the loader ran. **A stale fixture reads exactly like a
      broken pipeline**, and this row cost two sessions to that.

- `D18` ✅ `w1` `newcards` `build` `gate:none` — **Five cards still have no bespoke section**
      Gait, sustainedLoad, nutrition, metabolism, readiness. Biological age shipped
      with one; mental health did not. Cross-referenced to §C `S1`.

      ⚠️ Fixed (`adca807`), and **this row duplicates D1** — the two were written
      independently about the same thing, alongside §0's N3 and §C's S1.
      `InsightDetailView.swift:79-160` is exhaustive over all 26 `InsightID` cases;
      readiness is the one deliberate `EmptyView`, documented.

- `D16` ⬜ `w1` `data` `mech` `gate:phone` — **Score history is empty for five cards**
      readiness, sleep, energy, substanceImpact, fitness — the 90-day replay landed
      after the last export.

- `D23` ✅ `w1` `pipeline` `mech` `gate:none` — **`recompute()` recorded the derived series and wiped them a line later**
      The record loop sat above `invalidateDerivedCaches()`, which resets the
      store, so every sync recorded 18 results and erased them immediately. Found
      on the simulator: "Refresh complete — 18 insights" in diagnostics while the
      Data tab's Generated-insights row stayed absent.

      Fixed same session, order swapped. The general shape: **a cache-invalidation
      call at the end of a function silently owns everything recorded above it**.

- `D24` ✅ `w1` `pipeline` `mech` `gate:none` — **`InsightResult.appending(driverLines:)` silently dropped `subheadline`**
      The same rebuild-field-by-field shape that lost `invitesInput` on 2026-08-05.
      Fixed with `derivedOutputs` forwarding in `713ae0b`; the warning is now
      written at the site.

      There is still no compiler check for the next field added to `InsightResult`.

- `D25` ✅ `w2` `transparency` `build` `gate:none` — **Four weighted contribution sites leave `componentScore`/`z` nil**
      The component tier of Generated insights stays thin wherever the fields are
      left nil: they are optional and defaulted, so "do the sub-scores too" only
      covers the cards that fill them. Each nil on a weighted row is a card whose
      decomposition the deep dive also cannot show.

      ⚠️ This row said "~31 contribution sites" and was **materially overstated in
      both directions**. There are **44** `MetricContribution` sites outside tests;
      **24** leave both nil; **19 of those 24 are weight-0 rows where a sub-score
      would be a lie** — several already carry comments saying exactly that
      (`HeartAgeAnalyser.swift:164-167`, `MetabolismInsight.swift:396-398`).

      Only **four** weighted sites are real gaps: `BloodPressureEstimator.swift:1010`,
      `HealthWatch.swift:1446`, `SubstanceResponseAnalyzer.swift:787`,
      `BiologicalAgeModel.swift:929`. `ScoreBlend.swift:160-167` already fills these
      at the funnel for the seven files that route through it. So this is a
      four-site fix, not a sweep.

      Same fields feed backlog #38's decomposition, so the fix pays twice.

- `D26` ✅ `w0` `pipeline` `mech` `gate:none` — **`DiagnosticsLog` cannot be read from outside the app**
      It is in-memory for Settings ▸ Troubleshooting only, with no os_log mirror,
      so `simulator.sh logs` cannot see it and a session debugging the app has to
      click through the UI to read its own diagnostic line. Cost two build-launch
      cycles this session.

      A DEBUG-only os_log mirror would make `simulator.sh logs` sufficient.

- `D27` ✅ `w2` `data` `design` `gate:none` — **Raw-field rows render metadata as measurements**
      (UI/UX, Data tab): "How it was measured — 0", "Device model ID — 16",
      "Measurement categ… — 1" read as nonsense readings; long names
      ("Environmental Sound Reduct…", "Daily readiness temperature de…") truncate
      with no way to read the full name on the row.

      The session-27 "32.00 years" class, still visible in the Not-yet-sorted group.

- `D28` ✅ `w2` `data` `mech` `gate:none` — **Oura's stress fields sit in "Not yet sorted"**
      `Daily readiness score 58`, `Day summary "stressful"`, `Stress high 9900`,
      `Recovery high 0`, `Daily resilience level "limited"` render unsorted and
      unexplained at the bottom of the Data tab, while N1 (stress research) names
      exactly these fields as the free comparison.

      Sorting them into a labelled group is cheap and makes N1's raw material
      visible.

- `D29` ⬜ `w3` `data` `design` `gate:decision` — **The Data tab has grown very long**
      (UI/UX): with the raw catalogue loaded it is dozens of screens of scroll; the
      search field is the only fast path and a reader has to know a field's name to
      use it.

      Consider collapsed-by-default raw groups, or an index.

- `D30` ✅ `w0` `rules` `mech` `gate:none` — **`scripts/bash-workdir-hook.sh` makes any worktree-isolated agent unable to commit**
      ⚠️ It rewrites every shell call to `cd "$CLAUDE_PROJECT_DIR"`, which is the
      *main* repo, so a worktree agent's git calls are refused by the isolation
      guard and its relative paths hit the wrong tree. Two agents lost their commit
      this session; one lost it entirely and its doc had to be rescued by hand.
      Hook fixed on 2026-08-06 to read the payload's `cwd` — **the fix only takes
      effect for agents spawned after it lands on `main`**.

      Found live, `docs/setup-review-2026-08-06.md` finding 1.

- `D31` ✅ `w0` `rules` `mech` `gate:none` — **`.claude/settings.json:24` allows the hook's `cd` prefix only for the Linux path**
      The Mac and worktree prefixes have no allow entry. One-line fix, left for a
      main session because settings.json was outside the review agent's permit.

      Same review.

- `D32` ✅ `w0` `rules` `mech` `gate:none` — **Concurrent worktree agents collide on shared scratchpad filenames**
      One agent's git-helper script was twice rewritten by other agents to point at
      *their* worktrees with the safety check removed. Every agent that commits
      must self-check `git rev-parse --show-toplevel` immediately before staging.

      Observed 2026-08-06.

- `D33` ✅ `w2` `testdebt` `mech` `gate:none` — **`ContributionSummary.bodyMeasurements` and `.screenTime` are tested nowhere**
      `ContributionSummaryTests.swift:223`'s hand-built list has 7 of 9 route
      factories, so three "across every route" sweeps silently exclude two. The
      CardVisibilityTests closed-set lesson, live again.

      `docs/test-audit-2026-08-06.md` #1.

- `D34` ✅ `w2` `devices` `mech` `gate:none` — **Whoop never got the 2026-08-04 timezone fix**
      `WhoopResponseParser.parseSleep:89` takes no calendar and defaults
      `Calendar.current`; its only sleep test never asserts onset, so the bug class
      that hit Oura is untested here.

      Test audit #2.

- `D35` ✅ `w2` `export` `mech` `gate:none` — **The export has no decode round-trip anywhere**
      `HealthDataExportTests` is all `json.contains(...)`, and one test titled "must
      survive the round trip" never decodes. This is exactly the 2026-08-05 import
      shape-mismatch defect's shape.

      Test audit #5.

- `D36` ✅ `w2` `testdebt` `mech` `gate:none` — **Skip-guards that can hide forever**
      `SuggestionTests:254,278` use `XCTSkipUnless` on fixture drift;
      `ContributorDepartureTests:27–31` skip every model whose metrics aren't in
      GoldenDataset's five, so Fitness/BodyComp/BP have never been checked against
      the departure panel.

      Test audit #3, #4.

- `D37` ✅ `w2` `testdebt` `mech` `gate:none` — **Assertions that cannot fail**
      `NewCardTests:395` percentile 45±12 under a "just under 50" comment;
      `SleepQualityStagesTests:115` ±12 on a 0–100 score; `MetabolismTests:113`
      disjunctive assert; `CompositionVelocityTests:95` self-referential;
      `ScoreHistoryReplayEquivalenceTests:180–185` runs ReadinessInsight twice where
      a third model was intended.

      Test audit #7–9.

- `D38` ✅ `w0` `rules` `mech` `gate:none` — **Session-start cost is dominated by `docs/activeContext.md`**
      4,135 lines of 28 sessions' narrative, read every session. `handover.md` now
      instructs moving superseded sections to
      `docs/archive/activeContext-history.md`; the first actual pruning needs a
      main session.

      Setup review finding 2.

- `D40` ⬜ `w3` `newcards` `hard` `gate:decision` — **Heart Health's one derived figure is computed in the view, so it cannot be declared**
      The 2026-08-06 derived-series sweep covered all 18 registered models;
      seventeen are complete. Heart Health is the exception: heart-rate *recovery* —
      its bespoke section, and the one cardiac marker with a published fixed
      threshold — is evaluated by `HeartResponseModel` inside `InsightDetailView`,
      never reaches `InsightResult`, and therefore gets no series, no factor row and
      no Data-tab page. Moving the call into `HeartHealthInsight.evaluate` is the
      fix; it changes what the card reports, so it needs its own brief rather than
      being folded into a rendering pass.

      Named in a `MARK` comment at `HeartHealthScore.swift`'s result site so it
      cannot be mistaken for an omission.

      ⚠️ Accurate, but the framing above implies the metric is absent from the
      score and it is not. `HeartHealthScore.swift:246-247` **already reads
      `.heartRateRecovery` as a weight-bearing supporting term**. What never
      reaches `InsightResult` is the *derived recovery figure and its
      Cole-threshold band*, because `HeartResponseModel.evaluate` is called in the
      view at `InsightDetailView.swift:1511`. The gap is declaration and
      presentation, not scoring.

- `D41` ✅ `w1` `calendar` `hard` `gate:none` `ask` — **Work impact scores the calendar as well as the body, 2026-08-06**
      The reader overruled the weight-0 answer in the same session that shipped it:
      *"I want all inputs to carry at least some weight, thats the entire point. If
      i have had 10 meetings in a day, how would that not leave me impacted and
      drained?"*

      The card's number now blends **exposure** (the gap between busy and quiet
      working days, and the heaviest single day — both as multiples of the reader's
      own typical working day, never an invented band) with **response** (the same
      RHR/HRV/sleep comparison as before). Response carries 65% at full contrast
      and 30% at none, so a body difference measured across a flat calendar is
      discounted rather than blamed on work; the calendar carries the rest, and no
      calendar row is at weight 0 any more. Four quadrants — carrying / absorbing /
      unexplained / quiet — are named in the headline and the first driver line.
      `work-impact-v2`; **travel drain deliberately stayed v1**.

      `WorkImpactModel.Quadrant` carries the design; new
      `ScoreBlend.blend(metrics:factors:metricShare:)` is the shared machinery.
      **Open, and named at the site:** both exposure facets are ratios, so they are
      scale invariant — a diary that is heavy *every* day reads light. The real fix
      is scoring this fortnight against the reader's own trailing year once
      `workExposure` has a history, not a constant.

- `D42` ◐ `w4` `norms` `hard` `gate:decision` — **"How you compare" still has no norms for anything a card derives, and cannot**
      The section now *names* what it could not compare ("Nothing to compare these
      against") instead of omitting it silently, which was the honest half. The
      other half — comparing a reader against other readers of this app — is A4
      (crowd-sourced norms) and is the only route to an actual centile for meeting
      hours or a pooled departure.

      Do not invent one; standing rule 1.

      ⚠️ Gated on the three crowd-norms rulings the reader still owes: per-metric
      opt-in rather than one blanket switch; the minimum cohort size per age band
      and sex before any centile is shown; and whether contributing is automatic
      once you consume, or a second separate choice.

- `D39` ✅ `w1` `export` `mech` `gate:none` — **Logged bleeding days never reach the export**
      ⚠️ The app's full export never passed `cycles:` to `HealthDataExport`, so the
      key was always `[]` — the Data tab showed the cycle log and "export my data"
      silently omitted it. The exhaustive `exportKey(for:)` switch cannot catch
      this: it enforces that a domain *names* a key, not that the payload is
      populated, which is the same gap `HealthDataExportTests` was written to close
      for the other domains.

      Found in passing by the B7 agent, 2026-08-06.

      ⚠️ **Fixed by `42458b8`** — `DataExportView.swift:293` passes
      `cycles: model.cycleDays`, and that is the only `HealthDataExport(`
      construction site outside tests. Scheduling this would cost a session for
      nothing. The *general* gap it names is real and still open: the switch proves
      a key exists, never that it is filled.

- `D43` ✅ `w1` `data` `build` `gate:none` — **The app announces a discovery for data it then throws away**
      ⚠️ `AppModel.observeArrivals` records what *arrived* and runs **before**
      `partitionedVitals()` sanitises, so a reading outside its metric's
      `plausibleRange` is announced as "New since you last looked" and then dropped
      — the reader's single vitamin A entry does exactly this. The Data-tab row now
      appears regardless (fixed 2026-08-06), so the symptom is covered, but **the
      ledger is still recording arrivals that never became data**, which will
      mislead any future reader of it. Decide: observe after sanitising, or record
      the drop alongside the sighting so the row can say "arrived, but outside the
      plausible range".

      Found while fixing the discovered-row bug.

      ✅ **RULED 2026-08-07: record the drop beside the sighting.** The reader
      chose keeping the observe-before-sanitise order and storing *why* a reading
      was dropped, so the row can say **"arrived, but outside the plausible
      range"**. Their reasoning matches the app's habit of naming what it
      withheld (`CoverageGate`), and it keeps the honest signal that something
      *did* arrive — which is what matters when a device starts sending garbage.
      Observing after sanitising was rejected because a metric arriving
      persistently out of range would then look identical to nothing arriving,
      hiding a real device fault.

- `D44` ✅ `w0` `pipeline` `mech` `gate:none` — **`load-real-export.sh` was half a no-op on any container that had run the app**
      `loadCachedOther` prefers `synced_other.hirc`, and the loader deleted only
      `synced_samples.hisc` — so 177,000 raw rows were written to a file nothing
      read, and the app kept showing the previous run's catalogue. Cost a full
      simulator cycle to find.

      Fixed same session; both compact caches are deleted now.

- `D45` ✅ `w1` `chrome` `hard` `gate:none` — **Sleep Duration's trend chip and its detail describe different things, side by side**
      On Work impact the row reads "Holding steady · higher is better · 22% of this
      score" while its own detail says "about the same on your busier working days
      — 6.7 against 7.4 — which is 0.2 SD worse". The chip is the metric's *trend*;
      the detail is the *busy-versus-quiet contrast*. Both true, adjacent, and they
      read as a contradiction — which erodes trust in the numbers around them.
      Either the chip belongs elsewhere on a contrast card, or it has to name which
      question it answers.

      Seen on the reader's own phone, 2026-08-06.

      ✅ **RULED 2026-08-07: the chip names its question.** Keep both figures,
      but the chip states which question it answers — "90-day trend: holding
      steady" against the detail's busy-versus-quiet contrast. The reader chose
      this over dropping the chip on contrast cards because **it generalises to
      every contrast card rather than fixing one screen**.

- `D46` ✅ `w3` `transparency` `build` `gate:none` `ask` — **Every "not enough yet" gate is invisible**
      The reader's point, and the calendar review section already does it right —
      *"It needs 10 before that figure means anything"*. Nothing else does: 3 cycles
      before a phase prediction, 3 worn days before an effort dose, 5 days per week
      before a norm contribution, 2 contributors before a replayed score point. A
      reader who hits one sees an absence and cannot tell "no data" from "not enough
      yet" — and the second is a reason to keep going.

      Reader, 2026-08-06: *"it needs to be mentioned appropriately for transparency,
      so users know why things are - or are not showing in the app"*

      ⚠️ `CoverageGate` supplies the sentence shape, not the plumbing. Its only
      producers are `EffortIntensityModel.swift:112` and `CardioTrajectory.swift:27`,
      both section-level, and **nothing in the app target references it**. Anything
      written as "pairs with CoverageGate" is describing a vocabulary, not a
      delivery mechanism.

- `D47` ✅ `w0` `rules` `mech` `gate:none` — **A comment claims a compiler enforcement that does not exist: `DataTabView.detailPage(for:)`**
      `HealthInsights/Features/Data/DomainDataScaffold.swift:19` — and the matching
      rule in `docs/data-conventions.md` — claims "DataTabView.detailPage(for:) is
      exhaustive over DataDomain — a new kind of data cannot ship without a detail
      page". **No such function exists anywhere in the tree.**

      The exhaustive switch is `DataTabView.section(for:)` at `:476` and it renders
      a Data-tab *section*. The detail page is reached by a `NavigationLink` written
      inside each section body, which is convention, not compiler-enforced. **A new
      domain could ship with a section and no page and every check would pass** —
      which is precisely the guarantee the comment tells the next session it
      already has. Either fix the comment or make the claim true.

- `B20` ✅ `w1` `export` `build` `gate:none` `ask` — **One export, containing everything — collapse the five surfaces**
      **The reader, 2026-08-07:** *"I hate having all these different export
      options in the 'Export my Data', just have one export option that contains
      everything. This should also include troubleshooting, and the data & model
      improvements. Currently there is no way to export Data and model
      improvement data, include that in the full export."*

      Measured against the files they exported today, there are **five** separate
      surfaces:

      | Surface | Size | What it is |
      | --- | --- | --- |
      | `health-insights-export-<id>.json` | 172 MB | the full export — 377,284 samples over 64 metrics, 108,611 unmodelled rows |
      | `Inventory-text-<id>.txt` | 66 KB | 64 modelled signals, 149 imported-not-modelled, coverage per signal |
      | `card_outputs-text-<id>.txt` | 60 KB | every card's rendered output |
      | `model_internal-text-<id>.txt` | 8 KB | model internals |
      | `troubleshoot_log-text-<id>.txt` | 39 KB | `DiagnosticsLog`, 231 entries |

      **What "everything" has to include that nothing exports today:**

      1. **Data & model improvement** — the correction record. `R4` is this and
         is still open: the AI guess, the reader's correction, and the artifact
         judged. `R3` shipped the artifact snapshot (`ce9f76f`); nothing carries
         the three layers into a file.
      2. **Troubleshooting** — `DiagnosticsLog`, today a separate text file.
      3. The four side files above, folded in as sections rather than downloads.

      ⚠️ **Do not lose what the side files do well.** The inventory's per-signal
      coverage table is the single most useful artefact this app produces for a
      session, and `card_outputs`/`model_internal` were each built as a
      *diagnosis instrument* and each found a live defect on its first use. Fold
      them into the one file as named sections; do not drop them.

      ⚠️ **Size.** The full export is already 172 MB and is zipped to 36 MB.
      Adding sections is fine; the zip is what the reader actually shares.

      Depends on `Q10`'s four fields and `R4` — the export cluster is already
      touching these files, so sequence behind it rather than in parallel.

- `D51` ✅ `w0` `rules` `mech` `gate:none` — **`simulator.sh` let one worktree agent screenshot another's build, silently**
      **Closed 2026-08-07, same day it was found.** `SIM_DERIVED_DATA` defaulted
      to one shared path, every worktree installed the same bundle id onto the
      same simulator UDID, and shots were named `<short-sha>-<time>.png` — and
      the sha is **identical across worktrees built from the same base commit**.
      So agent A could screenshot agent B's build with nothing in the output
      saying so.

      ⚠️ **It cost a worktree agent several rounds.** It verified its chevrons,
      then two later screenshots showed them gone. The giveaway was a Blood
      Pressure card still rendering `= No change` — a string that agent's own
      commit had already replaced.

      **The shape worth carrying: a screenshot that is confidently of the wrong
      build is worse than no screenshot.** It is evidence pointing the wrong
      way, and this repo uses screenshots for exactly the claims tests cannot
      make — two cards shipped invisible on 2026-08-03 with green tests, green
      CI and a successful install.

      Fixed two ways, because one was not enough: a per-worktree derived-data
      slot and shot prefix (`--git-dir` vs `--git-common-dir`, the same
      canonical test `verify.sh` uses), **and `shot` now prints the installed
      binary's mtime on every call**. The first stops the collision; the second
      means a stale or foreign install is *visible* rather than deduced from
      noticing a string you thought you had changed.

- `D52` ⬜ `w2` `pipeline` `mech` `gate:decision` — **The diagnostics mirror is INFO-level, so everything but failures is hidden outside `simulator.sh`**
      `DiagnosticsLog.mirrorToUnifiedLog` maps ok/null/info to `Logger.info` and
      only `fail` to `Logger.error`. `simulator.sh logs` passes `--info` so it
      sees everything — but anyone reading the device's log through Console,
      `log collect` or a **sysdiagnose** sees only the failures unless they know
      to enable INFO.

      Two options, and it needs a deliberate choice rather than a default:
      **(a)** document the `--info` requirement in `docs/deployment.md`;
      **(b)** move the non-fail levels to `Logger.notice`, which is persisted by
      default. ⚠️ `DiagnosticsLog`'s own privacy header already assumes
      persistence — it names sysdiagnose explicitly — so **(b) does not change
      the stated posture**. But it does raise log volume, and a full sync writes
      a line per imported metric.

- `D59` ✅ `w1` `chrome` `hard` `gate:none` `ask` — **No card is ever hidden for want of data — the visibility filter is gone**
      **The reader, 2026-08-07:** *"No cards should ever be hidden if there is
      not enough data."* Ruled after they asked *"Where is the travel card
      gone"* — and it had gone, that same day, because of a fix earlier in the
      session.

      **The chain, because it is instructive.** Travel drain was saying "Connect
      your calendar" to a reader who had connected it (three `nil` paths, one
      message). The fix routed the waiting case through a new `waitingOn()`,
      which sets `invitesInput: false` — **correctly**, because a coverage gate
      is not an invitation and there is nothing to give. But `isWorthShowing`
      read `primaryValue != nil || !unmetRequirements.isEmpty ||
      isAwaitingTodaysData || invitesInput`, so **all four went false and the
      card disappeared from the list.**

      ⚠️ **`CardVisibilityTests` did not catch it, and the reason is the useful
      part**: its fixture is a card with *nothing at all*, which returns
      `.noCalendar`, still invites input, and is still visible. **The invisible
      state was "connected but still counting"** — between the two states
      everybody tests.

      Fixed twice over: `isWorthShowing` is unconditionally `true`, and
      `CalendarGateHonestyTests` covers the connected-but-counting state
      directly. Canaried — restoring the filter fails 3 of 6.

      Standing rule 2 rewritten. The superseded filter and its reasoning are
      quoted verbatim in `isWorthShowing`'s doc comment, because that reasoning
      was **sound about the job it was reasoning about** — a card with nothing to
      say could once only render "no data yet". `CoverageGate` changed the
      premise, not the taste.

- `D60` ✅ `w1` `export` `hard` `gate:none` `ask` — **The export carried no score history at all, so the app could never learn from itself**
      **Found 2026-08-07 by opening the reader's own export** after they asked to
      *"take everything we can learn based off correction data and grounding
      data"*. **All 18 cards exported `history: []`** while SwiftData held the
      rows.

      **Cause.** `AppModel.scoreHistory(for:)` is a **lazy view cache** — it
      returns `[]` and queues a background 90-day replay when a card's chart has
      not been drawn, because doing eighteen replays on the way to a first frame
      is too slow. Correct for a view; **wrong for an export**, which asks about
      every card at once and waits for nothing. Two call sites in
      `DataExportView` did it: `buildFullExport()` and `prepareDocuments()`.

      ⚠️ **This is D39's class exactly** — the key existed, the data existed, the
      payload was empty — and no InsightKit test can catch it, because
      `HealthDataExportTests` builds its own bundle rather than going through the
      caller. So the guard is a `verify.sh` lint, canaried.

      ⚠️ **Why it mattered more than one empty key.** With no exported history
      there are **no prediction-versus-actual pairs**, so nothing on a server —
      or in a later session — could grade any model. It is the reason the
      reader's ask could not be answered from the file they sent.

      Fixed with `AppModel.storedScoreHistory(for:)`, which reads SwiftData
      directly. ⚠️ `scoreHistoryIsPending` deliberately still reads the lazy
      cache: it answers *"is the replay running"*, which is a fact about the
      cache, and reading the store would always say no.

- `D61` ⬜ `w1` `export` `mech` `gate:none` `ask` — **The reader's export predates every model-improvement key — a fresh one is needed**
      The export supplied 2026-08-07T07:09Z is **schemaVersion 4**, from build
      `0.1.0 (398)` / commit `25a276b`. Every key the reader asked to learn from
      is **absent, and could not have been present**:

      | Key | Added by | Landed |
      | --- | --- | --- |
      | `feedback`, `connections`, `predictionOutcomes`, `suggestionDismissals` | `Q10` | schemaVersion 5, same day |
      | `improvements` (the three-layer correction record), `reports` | `B20`/`R4` | schemaVersion 6, same day |
      | `sickDays`, `tags` | `B11-4`, `B12-1` | schemaVersion 6, same day |
      | `calendarEvents` | — | ⚠️ **deliberately excluded**, and `D50` says re-decide it |

      **So no calendar-classification corrections, no feedback ledger and no
      prediction outcomes exist in any file yet** — not because they are not
      recorded on the phone, but because the export that carries them shipped
      hours after this one was taken.

      **What the file did carry:** 12 grounding facts (latest value per kind, not
      a history), 50 systolic and 50 diastolic cuff samples in `samples`, 377,284
      samples over 64 metrics, 108,611 raw rows — and, because of `D60`, zero
      score history.

      **Next step is a fresh export from the current build**, after which the
      correction and outcome data can actually be studied. ⚠️ **Ask for it; do
      not infer the corrections from anything else.**

- `D53` ✅ `w1` `chrome` `mech` `gate:none` `ask` — **The `+` add menu is cut off and has to be scrolled**
      **The reader, 2026-08-07:** *"When you click the '+' button, it gets cut
      off and you have to scroll through the list of add options.. make it
      stretch the length of the screen instead, why cut it short if you don't
      need to?"*

      The master add list is generated from `InputKind`, so it grows every time
      a new input ships — which means this gets worse on its own. A fixed or
      medium detent on the sheet is the likely cause; it wants the full height,
      or a detent that fits its own content.

      ⚠️ **Look at it on the simulator, do not reason about it** — sheet detents
      and safe-area insets are exactly the class no test can catch.

- `D62` ✅ `w1` `pipeline` `hard` `gate:none` `ask` — **The sync never finished, because it waited on twelve on-device model calls**
      **The reader, 2026-08-07:** *"Data no longer showing in the app"* — with a
      diagnostics log carrying `Refresh started` and **no `Refresh complete` at
      all** five minutes later, plus a 12.75 s main-thread block and hundreds of
      sub-second stalls.

      **Cause.** `performRefresh` **awaited** `refreshTagApplicability()` before
      its completion marker. `TagApplicabilityModel.resolve` loops **serially**
      over up to `perPassLimit` (12) tags, building a **fresh
      `LanguageModelSession` per tag** and awaiting a full on-device response for
      each — deliberately, so one bad answer costs one tag rather than the batch.
      Right for the classifier; fatal in front of the marker. The reader's Oura
      account returned **12 enhanced tags**: twelve sequential model round trips,
      the first including model load, all before `lastRefreshedAt` moved and
      before the cards were told the sync had ended.

      ⚠️ **The comment above the call was true and the code did not honour it.**
      It said this *"can only improve a heading — it can never be the reason the
      Tags section is empty or late."* Nothing said it could not be the reason
      **everything else** was.

      ⚠️ **The false lead, recorded because it was convincing.** The same log
      showed `Derived: 15 series … from 20 results`, against **163** on a
      simulator loaded with the same export — which reads as the models having
      stopped scoring. They had not: that line fires **10 seconds before**
      HealthKit returns, so it is the launch pass over a cold cache. **The
      MISSING line was the finding, not the small number.**

      Fixed by detaching it. Guarded by a `verify.sh` lint scoped to
      `performRefresh`, canaried both ways — including a first version that
      matched its own fix.

- `D63` ⬜ `w2` `testdebt` `mech` `gate:none` — **The app-target tests are flaky under concurrent load, and the failure names no cause**
      The app-target target landed 2026-08-07 (`D5`) and **passes on a quiet
      machine**: 14 tests, green. On the evening of 2026-08-07 — with ten to
      twelve worktree agents building concurrently, plus a simulator running the
      reader's real export — it failed repeatedly and **differently each time**:
      `CardRoutingTests` reporting **zero tests executed** with the host killed,
      `CardRenderSmokeTests` failing, `AppModelStateTests` passing throughout.

      ⚠️ **It blocked a reader-reported outage fix (`D62`) at the pre-push gate,
      and it made me diagnose it wrong twice.** The wrong turns are the useful
      part, so they are recorded rather than tidied away:

      1. *"The simulator holds the reader's export, so the app cannot finish
         hydrating inside a test host."* **Wrong** — the container measured 12 MB.
      2. *"Give the app tests their own simulator."* **Wrong** — the second
         device would not boot under load (`launchd_sim … could not bind to
         session`), and the attempt made the run worse.
      3. *"It is pre-existing, so push past the gate."* **Wrong, and the most
         dangerous of the three.** A `git stash` comparison appeared to prove the
         failure was not mine; it was contaminated by the container state at the
         time. Running the same class on `HEAD~1` **and** `HEAD` afterwards
         showed **both green**.

      **The gate was right to block on every occasion, and the failure was real
      flakiness rather than a real defect.**

      ⚠️ **A red gate that is usually environmental trains a session to push
      through a real one** — which is exactly what nearly happened, on a fix for
      an outage the reader had already reported. The fix is to make the failure
      *say* it is a resource problem: report machine load, or retry once before
      failing, rather than printing *"No `error:` line — the host may have
      crashed"* and leaving the reader to guess.

- `D54` ◐ `w1` `pipeline` `hard` `gate:none` `ask` — **Hangs, and nothing is watching for them**
      **The reader, 2026-08-07:** *"Can you enable hang detection on the
      emulator, to look for hangs? I see so many hangs..."*

      ⚠️ **This is a repeat symptom, and the last one was real and large.** On
      2026-08-06 the reader reported *"it hangs the UI/UX and the app becomes
      unresponsive"*; the cause was `recompute()` running the full 18-model pass
      on the `@MainActor`, measured at **2.36 s over 380k samples, from 33 call
      sites**. That was fixed by detaching it behind a generation guard — and
      the reader is still seeing hangs, so either something else blocks the main
      thread or the fix did not cover every path.

      **What to build, and it is three separate things — do not conflate them:**
      1. **Detection in the app**, DEBUG-only: a main-thread watchdog that logs
         through `DiagnosticsLog` when a runloop turn exceeds a threshold, so a
         hang appears in `simulator.sh logs` with a stack rather than as a
         feeling. This is the one that makes the others findable.
      2. **Xcode's own instrumentation** — the Thread Performance Checker
         (Diagnostics ▸ Runtime Sanitization) reports main-thread hangs and
         priority inversions on the simulator.
      3. **`MetricKit`'s `MXHangDiagnostic`** for the reader's real phone, which
         is where the hangs they are reporting actually happen. The simulator
         cannot reproduce a device-only hang, and this is the only route to one.

      ⚠️ **Then find the cause, not just the detector.** The likely suspects,
      given this codebase: any `@MainActor` model doing work over the 377,284-row
      sample set, SwiftData fetches on the main context, and view bodies that
      call into a model rather than reading a stored property (the observation
      trap `docs/data-conventions.md` names).


      ◐ **Part done 2026-08-07. What the agent left, in its own words:**
      Two of its three instruments are built and shipped: the DEBUG-only MainT
      hreadWatchdog (main-runloop heartbeat, 250ms notice / 1000ms hang, repor
      ted through DiagnosticsLog so it reaches simulator.sh logs) and HangDiag
      nosticsReporter (MetricKit MXHangDiagnostic + MXMetricPayload hang histo
      gram + crash diagnostics, device-only). Item 2, Xcode's Thread Performan
      ce Checker, is NOT done — it is a scheme setting and project.yml has no 
      schemes block, and that file is shared with eleven concurrent agents so 
      I left it alone rather than risk the conflict. The exact YAML is in newR
      ows. Also unfinished by design: neither detector has yet been observed f
      iring — the watchdog was never seen catching a real stall, because repro
      ducing one needs a seeded simulator and the machine is not quiet. And th
      e row's second half, 'then find the cause, not just the detector', is on
      ly partly answered: one real cause was found and fixed (D58's memoized r
      e-entrancy, which aborted rather than hung), and the insight pass was sh
      own not to be it.

- `D55` ✅ `w1` `pipeline` `build` `gate:none` `ask` — **`SyntheticSeed` is not kept in step, so new cards and fields are empty on the simulator**
      **The reader, 2026-08-07:** *"looks like each time we make a new feature,
      we should be updating the simulate data feature to support it, like i see
      multiple new cards where there is no data, and i see lots of data fields
      that have no data..."*

      ⚠️ **Exactly the class this repo keeps paying for, one surface further
      along.** `DataDomain` makes a new kind of data appear in the Data tab or it
      does not compile; `InputKind` does the same for input surfaces;
      `MetricExplainer` does it for descriptions. **`SyntheticSeed` has no such
      rule**, so every new `MetricType` and every new card ships with an empty
      simulator — and the simulator is this project's only way to *see* the app
      without the reader's phone.

      **It is worse than cosmetic.** Two cards shipped *invisible* on 2026-08-03
      with green tests, green CI and a successful install, and the simulator is
      what catches that class. A card that renders empty because the seed lacks
      data is indistinguishable, on screen, from a card that is broken — so the
      one tool that falsifies UI claims quietly stops working as more features
      land.

      **The fix is the same shape as the others: make it exhaustive.** A seed
      built over an exhaustive `switch` on `MetricType` (and on `InsightID` for
      card-level state) means a new metric does not compile without either seed
      data or a stated reason for having none — *"nothing writes this, see §B5
      #37"* is a legitimate answer and should be written down, not inferred from
      an empty chart.

- `D56` ◐ `w1` `sleep` `hard` `gate:none` `ask` — **Sleep is not timezone- or travel-aware, and the reader has now actually flown**
      **The reader, 2026-08-07:** *"Sleep needs to be timezone and travel aware,
      yesterday i was in the phillipines, today i am back in australia, i also
      slept on the plane, all these things need to clearly report in MY current
      timezone and all show correctly."*

      ⚠️ **This was predicted, in writing, and the prediction has now come
      true.** The 2026-08-05 day-stamp work recorded: *"the two conventions
      reconciled **by accident** — `startOfDay(midnightUTC(D))` is `D` at a
      non-negative offset and `D−1` at a negative one — so the reader's first
      flight west would have sheared the lanes one day apart, silently."* The
      UTC-pinned formatters were removed and `DayStamp.local` is the rule, so
      the ingestion half is fixed. **What was never handled is a day whose start
      and end are in different zones.**

      **Three distinct problems, and they need separating before any code:**

      1. **Which zone is "my" zone for a past night?** The reader's instruction
         is explicit — *"report in MY current timezone"*. So a night in Manila
         viewed from Sydney must render in Sydney time, which means the stored
         instant is canonical and the *display* converts. Check every place a
         night is rendered: if any of them formats with a stored zone rather
         than the current one, that is the bug.
      2. **A flight night has no single day.** Sleep beginning at 23:00 in
         Manila and ending at 07:00 in Sydney spans a 2-hour offset change, so
         `DayStamp.local` gives a different answer at each end and the duration
         is not `end − start` in wall-clock terms. ⚠️ **The duration in
         *elapsed* terms is correct and the wall-clock one is not** — decide
         which the card reports and say so on screen.
      3. **Plane sleep is fragmented and mid-day.** It will read as a nap, or as
         a fragment, and `P27`'s fragment work (median ~11 min segments) decides
         what happens to it. A night that is genuinely two sleeps in one
         calendar day is the case `SleepNights` was never built for.

      ⚠️ **The app captures no time zone on any health reading** — recorded at
      §B2 #15 — so a calendar entry is currently the only thing on the phone
      that knows the reader moved. `CalendarModel.timeZoneChanges` already finds
      those changes and Travel drain reads them; **sleep does not**. That is the
      cheapest available join and it should be the first thing tried.

      ⚠️ **Do not fix this by pinning anything to UTC.** `verify.sh` bans the
      shape, and the ban exists because a UTC-pinned test of UTC-pinned code
      agrees with itself while losing every Oura bedtime in a two-year history.


      ◐ **Part done 2026-08-07. What the agent left, in its own words:**
      Problems (2) elapsed-vs-wall-clock and (3) plane sleep are closed - both
       are decided, computed and stated on screen in the new SleepTravelSectio
      n. Problem (1) 'which zone renders a past night' is only HALF closed. Th
      e new section renders correctly in the reader's current zone, but every 
      OTHER render site in the app still formats the stored value, which was b
      aked at ingest in whatever zone the phone was in that night. The app-wid
      e fix touches AppModel and is not safe to make inside a parallel wave - 
      the precise instruction is in the summary.

- `B21` ✅ `w3` `calendar` `hard` `gate:none` `ask` — **Jetlag on the Travel card: how much, how it lands, and how long to recover**
      **The reader, 2026-08-07:** *"One thing I want on the travel card: A way to
      calculate jetlag, and how that impacts you, and how long it takes to
      recover from it."*

      Three questions, and they are answerable in increasing order of difficulty:

      1. **How much jetlag.** The dose is not "hours flown" — it is **offset
         magnitude × direction**, and **eastward is harder than westward**
         because the circadian period is slightly over 24 h, so delaying is
         easier than advancing. A published rule of thumb is ~1 day of
         adjustment per time zone westward and ~1.5 eastward. `CalendarModel.timeZoneChanges`
         already yields the change and its signed offset — that is the input,
         and it is already computed.
      2. **How it lands.** Travel drain already compares the
         `TravelDrainModel.recoveryDays` after a change against ordinary days
         across its channels. **Jetlag is that same comparison keyed on signed
         offset rather than on "a trip happened"** — so it mostly reuses what
         exists, and the honest version reports per-channel deltas.
      3. **How long to recover** — the hard one. Recovery is *the number of days
         until the channels return to baseline*, which means fitting a return
         rather than assuming `recoveryDays = 4`. ⚠️ **`recoveryDays` is
         currently a constant and this row is what would replace it with a
         measurement.** With `minimumTrips = 2` on the reader's record, a fitted
         recovery curve is not supportable yet — **say how many trips it would
         take** rather than fitting one to two.

      ⚠️ **Depends on `D56`.** A flight night is exactly the night whose day
      boundary is ambiguous, so jetlag arithmetic built before travel-aware
      sleep lands will be computing on sheared days. Do `D56` first.

      ⚠️ **The published direction asymmetry must not become an unlabelled
      constant** — that is `B19`'s failure exactly (five constants setting a
      card's whole range, none from any literature). Cite it or derive it.

- `D57` ✅ `w1` `pipeline` `hard` `gate:none` — **The refresh takes 15.5 seconds on the real record — measured, not estimated**
      Measured 2026-08-07 on the simulator with the reader's own 2026-08-07
      export loaded. The app's own diagnostic line:

      > `Sync: Refresh complete in 15.5s — 377316 samples, 18 insights`

      ⚠️ **This is almost certainly what the reader means by "I see so many
      hangs" (`D54`).** The 2026-08-06 fix detached `recompute()` from the
      `@MainActor` — so the app should no longer *block* — but detaching work
      does not make it fast, and fifteen seconds of "Comparing today with your
      usual" on every launch reads as a hang whether or not the main thread is
      free.

      ⚠️ **And a launch screen is not an answer.** `D9` says cold-launch time
      against the real record is unmeasured and the launch screen may be
      covering 1 second or 15. **It is 15.5**, and that row can now be closed
      with a number.

      Where to look: the import loop logs per-metric counts (`ok: 73654 × Heart
      Rate`), so the 18-model pass runs over the full set each time. `RenderMemo`
      and `AppModel.memoized` exist; whether the *insight* pass reuses anything
      across the 18 models is the question — resting heart rate alone is read by
      seven of them.

- `D58` ✅ `w1` `pipeline` `hard` `gate:none` — **A SIGABRT crash report exists from today's simulator run**
      `~/Library/Logs/DiagnosticReports/HealthInsights-2026-08-07-190227.ips` —
      `EXC_CRASH (SIGABRT)`, "Abort trap: 6", no exception subtype and no `asi`
      payload, so the reason is not in the report's header and needs the
      backtrace read.

      ⚠️ **Provenance is genuinely ambiguous and must be established before
      chasing it.** Twenty-six agents were running in parallel worktrees when it
      was written, and until `D51` landed they all installed onto the same
      simulator UDID — so this may be any of their builds rather than `main`'s.
      **Reproduce on a quiet machine before diagnosing.** Recording it rather
      than acting on it, because a crash chased in the wrong build is worse than
      one left for an hour.

- `R60` ◐ `w3` `rules` `ultra` `gate:none` `ask` — **Seven research briefs commissioned 2026-08-07 evening — collect their outputs**
      In the reader's final minutes of the session they commissioned, verbatim
      or near: **cycle automation** (*"use ALL of the data… even recommend days
      we think are ovulation… reduce any or all input"*), **UI/UX** (*"Research
      good UI/UX that we can introduce into our app"*), **mental health + drug
      interactions** (*"what sources we can input… anti mental health drugs…
      if you are on lexapro, but have MDMA.. you could have a very bad few days
      after"*), **cuffless BP** (*"predictive cuff less blood pressure"*),
      **nutrition/metabolism bootstrapping** (*"surely we can derive something…
      even if its by asking them for static inputs"*), **wished-for features**
      (*"new high value features that people wish health apps had"*), and a
      **creative gap-hunt** (*"anything else we are missing"*).

      Plus a fifteen-topic evidence fleet covering: sleep influences, sleep
      debt, ideal bedtime, jetlag, supplements/DSLD, tag mapping, hang
      detection, accessibility, screen time evidence, sick-day type inference,
      social battery physiology, model accuracy presentation, location privacy,
      crowd norms, OCR dates, and sheet sizing.

      ⚠️ **All were running as agents when the session ended.** Their outputs
      land in the workflow/task results under this session's directory —
      `…/7f9c6881-3877-4d55-80a2-79bcf81103b5/tasks/` — and each is a markdown
      document intended for `docs/`. **The next session's first job on this row:
      collect what completed, save each under `docs/<topic>-research-2026-08-07.md`,
      and split the actionable findings into their own backlog rows.** An agent
      that died leaves its topic uncovered — re-run it from the brief in this
      row rather than assuming it finished.

      ⚠️ Two design docs already exist from earlier today and were **refuted** —
      `docs/energy-design-2026-08-07.md` and `docs/stress-design-2026-08-07.md`
      both carry not-build-ready banners. Redesign agents for both were also
      running. Do not treat the banners as stale without checking whether v2
      landed.

- `D50` ✅ `w1` `export` `mech` `gate:none` — **A new `DataDomain` can ship with an export key and no data, and no check notices**
      Found while diagnosing the reader's 2026-08-07 report that new data types
      *"aren't making it into exports by default"*.

      Three things are enforced today and the third has a hole:

      1. `DataTabView.section(for:)` is exhaustive over `DataDomain` — a new
         domain must render a Data-tab section. ✅ compiler.
      2. `HealthDataExport.exportKey(for:)` is exhaustive over `DataDomain` — a
         new domain must **name** an export key. ✅ compiler.
      3. `verify.sh` reads the parameter labels off `HealthDataExport.init` and
         fails when `DataExportView.buildFullExport()` does not pass one. ⚠️
         **This only checks arguments that ALREADY EXIST.** Nothing requires a
         domain to *have* an initialiser argument at all.

      So a new domain can ship with a section (forced), a key (forced), and
      **no data in the export** (not forced) — the reader's exact observation.

      ⚠️ **The live instance is deliberate, which is why nobody noticed the
      hole**: `calendarEvents` returns the `"unmodelled"` key and emits nothing,
      on the stated ground that a calendar event carries a title and a location,
      the most identifying strings this app holds. That reasoning is sound and
      is now **superseded by the reader's own later ruling** — `R5`'s two-tier
      sharing, both tiers on by default, where **Full** is *"the artifact plus
      the before/after categories"*. Re-decide it deliberately rather than
      leaving it inherited.

      The fix: make the check "every `DataDomain` either has an init argument
      the caller passes, **or** a declared exemption naming its reason", so the
      calendar exclusion has to be argued rather than silent. Same contract as
      the substance-shading and data-detail exemptions.

      ✅ **RULED AND BUILT 2026-08-07.** The reader: *"if they have full sharing
      your corrections enabled, it will be enabled for that future feature
      (server) and the export."*

      So `calendarEvents` now owns its own export key and is **the only
      tier-conditional key in the file**: the whole calendar — including the
      events never reviewed — travels at `.full` and at no other tier. Gated by
      the same switch that already governs `improvements`, deliberately: the
      reader made one choice and it has to mean one thing everywhere.

      ⚠️ **The risk assessment that justified the old exclusion is unchanged and
      still true** — titles and locations remain the most identifying strings
      this app holds, and the original reasoning is kept verbatim at the site.
      What changed is **who decides**. An unconditional exclusion was the app
      overruling a choice it had just handed the reader in `R5`.

      ⚠️ **The key is written even when empty.** An empty array reads as *"you
      have this turned off"*; an absent key reads as *"this app does not export
      calendars"*. Different sentences, and
      `testAnEmptyBundleStillCarriesEveryKey` is what holds the difference.

      The structural half — a lint so a future domain cannot ship with a key and
      no data — landed with the same row earlier.

- `D49` ✅ `w2` `data` `build` `gate:none` — **Three Data-tab sections open nothing — found by D47's new lint on its first run**
      `cycleSection`, `calendarSection` and `otherDataSection` in
      `DataTabView.swift` render a summary and contain **no `NavigationLink`**.
      `docs/data-conventions.md`'s second rule says every domain opens a
      read-only detail page; these three do not, and nothing noticed because the
      comment claiming a compiler guarantee named a function that never existed
      (D47).

      ⚠️ **Each carries a `// data-detail: exempt` comment naming THIS row, and
      the exemption is meant to be spent.** An exemption with no id is a
      permanent excuse. The lint holds for every future domain either way.

      - **cycles** — the reader can reach the detail via the Cycle tab, just not
        from the Data tab. A gap in the convention, not in the data.
      - **calendarEvents** — the review list lives on Work impact and Travel
        drain, so the events are reachable but not from where the reader asked
        for a consistent way in.
      - **unmodelled** — ⚠️ **this one may be correct rather than a gap.** It is
        not one kind of data but ~158 raw identifiers browsed by search in
        place; a single detail page for it would be a page of everything.
        **Decide deliberately rather than inheriting the exemption.**

      The reader's own words behind the convention: *"those menus need to follow
      at least a minimum convention of how those subpages are built… so I don't
      need to keep reprompting and checking for this every time."*

- `D48` ✅ `w0` `rules` `mech` `gate:none` — **`docs/card-sections.md`'s per-chart audit under-counts the charts**
      `docs/card-sections.md:764-779` lists **11** `ScrollableMetricChart` wrappers;
      there are **13**. `SleepOnsetChart.swift:88` and `DerivedSeriesChart.swift:84`
      are both missing, and the inline `Chart` at `DataTabView.swift:1188` is absent
      from the substance-shading census.

      Anything sized off that table under-counts the charts §B13 applies to — which
      is how a "sweep every chart" pass ships having missed two.

---

## §B9 — New cards

- `B9-1` ✅ `w3` `newcards` `build` `gate:none` `ask` — **Social battery**
      New card. Scope not yet specified — needs a brief before building.

      ⚠️ Still an open ruling as of 2026-08-07: the card is named and scoped
      nowhere. This is exactly the "ask a pull request's worth of questions
      *before* writing code" case — do not start it on the strength of the name.

      ✅ **SCOPED 2026-08-07 — the reader wants all three framings, not one.**
      Their words: *"I want all of the above! And of course, make sure any data
      points we derive go into the data tab, and they used appropriately in the
      weightings and 'what goes into this' charts. I want several bespoke charts,
      to show very good insights into people's 'social batteries'."*

      So the card carries three questions, not one:
      1. **Depletion from social load** — social contact (calendar events with
         attendees, meeting hours, formal vs casual) against how depleted the
         body looks afterwards. The exposure×response shape `WorkImpactModel`
         already uses, pointed at people rather than work — `ScoreBlend.blend(metrics:factors:metricShare:)`
         is the shared machinery and the four-quadrant naming is the precedent.
      2. **Capacity remaining today** — forward-looking social tolerance.
         ⚠️ **Overlaps Energy heavily, and Energy's own calibration is the open
         `B19` problem** — do not inherit its invented constants. If this cannot
         be said honestly, say so on the card rather than inventing a number.
      3. **Introvert/extrovert recovery pattern** — whether contact *restores*
         or *drains* this reader specifically. **The most novel of the three and
         the most honest at low n**, because "we cannot tell yet" is a real
         answer it can give.

      ⚠️ **Three explicit requirements from the same message, all binding:**
      every figure it derives becomes a `DerivedSeriesID` **and** appears in the
      Data tab (standing rule 10); those figures carry weights and appear in
      "What goes into this" and "How this is weighted" (rule 4); and it needs
      **several bespoke charts**, not one — `secondaryBespokeSection` exists
      alongside `bespokeSection` for exactly this.

- `B9-2` ⬜ `w3` `screentime` `build` `gate:needs:B10-1,B10-2` `ask` — **Screen time card**
      *"based on all the new screen time data we are supporting"*.

      ⚠️ The data it would draw is currently mis-attributed by day (B10-1) and
      the whole-week import is broken (B10-2). A card built on that ships wrong
      to the phone, so both bugs land first.

---

## §B10 — Screen time import via photo (OCR) — **two live bugs**

- `B10-1` ✅ `w1` `screentime` `build` `gate:none` `ask` — **Single-day import: the date is mostly wrong**
      The OCR *"correctly OCRs the data, and says it recognises the actual day
      and time on phone"*, and the **time** at the top is right — but the
      **day/date is mostly wrong**. So the parse succeeds and the date
      attribution fails.

- `B10-2` ✅ `w1` `screentime` `build` `gate:none` `ask` — **Whole-week scans are broken**
      *"this is no longer working, the model says the numbers do not add up."*
      A regression — it worked before.

      ⚠️ Because it is a regression, **bisect before rewriting**. Find the commit
      that changed the whole-week parse; rebuilding the parser from scratch on a
      "numbers do not add up" report risks fixing the wrong thing and losing the
      behaviour that already worked.

---

## §B11 — Symptom radar: the Sick days section

**The largest item in this brief.** A bespoke section on the symptom radar,
plus a new data source, plus a calendar-classifier change, plus a learning loop.

### ⚠️ EVIDENCE WARNING added 2026-08-07 — read before building the inversion

`docs/illness-detection-evidence-2026-08-07.md` surveyed 28 studies and two of
its findings bear directly on this section:

- **Roughly two-thirds of genuine infections never produce a clear
  physiological signal** (only 36.4% of PCR-confirmed cases showed a ≥3 br/min
  respiratory-rate rise; prospective PPV is 4–12%; the one RCT's physiological
  arm returned **zero** confirmed infections from 26 alerts).
- **The signal is non-specific systemic strain**, not illness — the same
  pattern precedes RA and IBD flares, and is reproduced by alcohol, hard
  training, poor sleep and travel.

So *"your pattern does not look like illness"* is **not** evidence that someone
was not ill — it is the expected reading for most real illness. **Recommendation:
compute the inversion and store it for the model, but never surface it to a
person as a judgement about honesty**, and hold it to a far higher bar than the
forward direction. This does not block the feature; it constrains what the
feature may *say*.

- `B11-1` ✅ `w3` `sickdays` `build` `gate:none` `ask` — **The section itself: two views, switchable — log and calendar**
      - **Two views, switchable: log and calendar.**
      - **Log view** — every day flagged symptomatic, with that day's score.
      - **Calendar view** — a month at a time, skippable between months, each day
        **coloured by symptom score** (green / yellow / orange / red).

      ⚠️ Open ruling, and it is the one that blocks the calendar view: **which
      per-day number colours it.** `SymptomRadarModel.timeline`
      (HealthWatch.swift:650) gives the single-day statistic; the card actually
      shows `verdict()` (:586), which takes max(today, CUSUM accumulation);
      `DataStore.scoreHistory` gives what the reader was actually shown but only
      since install. They disagree on exactly the carried-forward days the reader
      already complained about (*"why am I back at 99%"*). Pick one and say why.
      The log view is not blocked by this; only the colouring is.

      ✅ **RULED 2026-08-07: colour each day by its own statistic, and draw the
      accumulated episode as a band across the days it spanned.**

      The three candidate numbers disagree precisely on the carried-forward days
      the reader already queried (*"why am I back at 99%"*):
      `SymptomRadarModel.timeline` (HealthWatch.swift:650) gives the single-day
      statistic; `verdict()` (:586) takes max(today, CUSUM accumulation) and is
      what the card actually showed; `DataStore.scoreHistory` gives what was on
      screen but only since install.

      **The ruling takes the day's own statistic for the colour** — so each day
      is independently readable and a recovering day is not painted red by
      memory — **and draws the episode as a band**, so a multi-day illness still
      reads as a block rather than scattered days. More design work, chosen
      deliberately.

- `B11-2` ⬜ `w3` `sickdays` `build` `gate:needs:B11-1` `ask` — **The per-day page (reachable from either view)**
      - The **radar for that day**.
      - An **AI summary for that day**.
      - **The graph of all contributing data sources**, so the reader can see how
        each contributed.

- `B11-3` ⬜ `w3` `sickdays` `build` `gate:needs:B11-2` `ask` — **Estimated sickness, which the reader can correct**
      An **estimated sickness**, derived from all the data, **which the reader can
      correct** — sickness **type** and **severity** — *"similar to how you can
      correct a work or travel event (same concept - then we can learn from it)"*.

- `B11-4` ✅ `w3` `sickdays` `build` `gate:none` `ask` — **A sick day is a data source**
      *"since this 'sick day' is now a new data source, it should of course now be
      stored in the data section too, so all of this can be viewed and changed from
      the data section too."*

      ⚠️ This needs a new `DataDomain` case and a Data-tab section, and the
      compiler only enforces half of that: the exhaustive switch is
      `DataTabView.section(for:)` at :476, which renders a Data-tab *section*. The
      detail page is reached by a `NavigationLink` written inside each section
      body — convention, not compiler-enforced. (`DomainDataScaffold.swift:19` and
      `docs/data-conventions.md` both claim a `DataTabView.detailPage(for:)` is
      exhaustive over `DataDomain`; **no such function exists anywhere in the
      tree**.) A sick-day domain could ship with a section and no page and every
      check would pass.

- `B11-5` ⬜ `w3` `sickdays` `build` `gate:needs:B11-3` `ask` — **The learning loop — and the reader is explicit about scale**
      - Log **both** confirmations and corrections into data-and-model-improvement.
      - ⚠️ *"it is important for it to include all meta data for every known possible
        metric, for that day, so we can do full statistical analysis later and be able
        to profile sickness more effectively. I am not too worried about the data
        blowing up, because people do not get sick too often… but when they do, we
        want ALL health data from that day."*
        **So: a sick-day record captures the complete daily snapshot, deliberately.**

- `B11-6` ✅ `w3` `calendar` `build` `gate:none` `ask` — **Calendar integration as a feed: a 'sick' classification**
      - The **calendar AI classifier must be able to classify a day as 'sick'**, in
        the same dropdown as `meeting`, `reminder`, `travel`.
      - Misclassification must be correctable both ways — travel → sick, sick → work.
      - For a sick classification the **work/personal/not-sure** selector and the
        **formal/casual/standard** selector are **not relevant** and should not show.
      - It **does** need a **severity of sickness** selector.

      ⚠️ Not blocked on calendar persistence, whatever §0/§D say. Calendar events
      have been stored since 77847b2: `CalendarEventRecord`
      (PersistenceModels.swift:232) and `CalendarJudgementRecord` (:349) are both
      `@Model`s, registered in DataStore.swift:21's Schema, written by
      `DataStore.mergeCalendarEvents` (:485) from `AppModel.syncCalendar` (:350),
      read back by `loadCalendarEvents` (:473) into `AppModel.calendarEvents`
      (:249) and bound into the engine at InsightEngine.swift:89-93.

- `B11-7` ⬜ `w4` `sickdays` `ultra` `gate:decision` `ask` — **The fake-sick-day problem — the reader's own scientific objection, and the inversion**
      - *"I also need a way for us to reclassify if a 'sick' day, is actually a 'sick
        so I can get off work' kind of day.. because I don't want fake sick days to
        accidentally break our science models later.. where they are not sick,
        perfectly healthy, but a sick day is reported and we profile their healthy
        metrics."*
      - A calendar sick day **may** be flagged to add more info; **not mandatory**.
      - ⚠️ **An unconfirmed calendar sick day still enters the training set as a
        flagged sick day.**
      - **Then the inversion**: *"once we learn the correlation between a sick day
        calendar day, sick profiles radar map patterns.. we could then tell if they
        are not actually sick, and again flag it as a possible 'sick to get off work'
        day."*

      ⚠️ The evidence warning above constrains what this may **say**, not whether it
      is built. Compute and store it for the model; never surface it to a person as
      a judgement about honesty. Two rulings are still owed: **what confidence bar
      it must clear**, and **whether anything is ever surfaced to a person at all**.

- `B11-8` ⬜ `w3` `sickdays` `hard` `gate:needs:B12-1` `ask` — **Weightings**
      - **Symptoms must go into this card's weightings** — as must calendar sick days
        and other derived symptom scores.
      - Oura tags considered contextually (a sickness flag, etc) once §B12 lands.

- `B11-9` ⬜ `w3` `data` `mech` `gate:needs:B11-4` `ask` — **And: all of it reaches the data tab**
      *"All of this derived data must go into the relevant data tab sections."*

---

## §B12 — Oura tags (and equivalents from other sources)

- `B12-1` ✅ `w3` `tags` `build` `gate:none` `ask` — **Import tags, and let them contribute to the relevant cards**
      Import tags and **let them contribute to the relevant cards**: sickness or
      symptoms → symptom radar; activity → fitness; mental health → mental health.

      Worked example, the reader's own: a sport tag lives primarily in a **new
      'Tags' data section**, and *"whatever it is (eg Kayaking) will have an
      'applicability' of 'Activity & mobility'*".

- `B12-2` ⬜ `w3` `tags` `hard` `gate:needs:B12-1` `ask` — **Use AI to map tags to high-level categories — and it must scale to tags never seen before**
      ⚠️ **Use AI to map them.** *"maybe we should put these tags through AI, to
      determine their applicability. Like when they are imported, in the data
      section we use AI to map them to relevant high-level categories."*

      ⚠️ **This must scale to tags never seen before** — Oura supports custom tags,
      so a fixed lookup table is not an answer.

- `B12-3` ⬜ `w3` `tags` `mech` `gate:needs:B12-2` `ask` — **Activity tags become card candidates at the next review, not automatically**
      Those activity-related tags then become **candidates for cards at the next
      review** — not wired automatically.

---

## §B13 — Chart behaviour when panned off the data — **a new universal rule**

Per the 2026-08-07 code check this is **two changes in two places, not one** —
the brief reads as though it all lives in the shared chart component, and it
does not.

- `B13-1` ✅ `w1` `charts` `build` `gate:none` `ask` — **The header never disappears when panned off the data**
      - **Bug**: panning to a region with no data makes the *"Range over this period"*
        header **disappear**, and the UX **violently jumps to the top**.
      - **Required**: the header **never disappears**; it states that there is no data
        in this period instead.

      ⚠️ This is **host-side, not in the shared component**.
      `ScrollableMetricChart` already has an honest in-plot empty state
      (`emptyMessage` :29, `isEmpty` :32, overlay :110-116) and all 13 wrappers pass
      a real `isEmpty` predicate. The header that vanishes is a sibling `Card`
      rendered by MetricViewStrategy.swift:123 and hidden by the `if let summary` at
      :120 — same shape at :58 and :176 — and the violent jump is **content-height
      collapse** across MetricDetailView.swift:103-121 (:108–121). Fixing the shared
      component alone will not close this row.

- `B13-2` ✅ `w2` `charts` `build` `gate:none` `ask` — **`<` / `>` edge affordances that auto-pan to the nearest data**
      - **Improvement**: when no data is visible, show a **`<`** on the left edge to
        auto-pan to the nearest data point to the left, and a **`>`** on the right
        edge for the nearest to the right.

      This half *is* a single change in `ScrollableMetricChart.swift`, inherited by
      all **13** wrappers.

      ⚠️ Size it off the code, not off the docs: `docs/card-sections.md:764-779`'s
      per-chart audit table lists **11** wrappers when there are 13 —
      SleepOnsetChart.swift:88 and DerivedSeriesChart.swift:84 are both missing, and
      the inline `Chart` at DataTabView.swift:1188 is absent from the
      substance-shading census entirely.

- `B13-3` ✅ `w2` `charts` `mech` `gate:needs:B13-1,B13-2` `ask` — **Write the rule into the chart skill**
      ⚠️ *"I want this to be a rule for every graph in the app, add it to the graph
      building skill/rules."* → **`.claude/skills/add-chart/SKILL.md`**, as §9b.

---

---

## §B14 — Show that a card opens

- `B14` ✅ `w2` `chrome` `mech` `gate:none` `ask` — **A chevron on every card and every clickable section**
      Add a **`>`** to the far right of **every card and every clickable
      section**, to show it opens a sub-menu.

      Explicitly includes the **small cards on Today** (BPM, HRV, cardio,
      weight, sleep), not just the main cards.

      ⚠️ Narrower than it reads. Every `NavigationLink` inside a `List` already
      gets the system chevron free (`DataTabView.swift:321`,
      `SettingsView.swift:11`, `DomainDataScaffold.swift:63` and others), and
      every row-level link inside `InsightDetailView` already draws
      `chevron.right` explicitly (`:1480`, `:2440`, `:3242`, `:3585`, `:3613`).
      The genuinely missing chevrons are on **`InsightCard`**
      (`DashboardView.swift:301` — the one row shared by Today and Insights) and
      the **five `VitalsGlance` tiles** (`:267–281`). Two files, not a sweep.

      ⚠️ One ambiguity left in the ask: does `InsightSection`'s existing
      `chevron.down` (`InsightSection.swift:172`, which *expands* rather than
      navigates, with the rationale written at `:164–176` and in
      `card-sections.md:168–170`) satisfy "`>` on every clickable section"? If
      not, that reverses a documented decision and adds a third file. The two
      missing chevrons above do not wait on that answer.

---

## §B15 — Card trend indicators — **a new universal rule**

Half of this rule already renders. **Down** → down arrow + the negative number,
and **Up** → up arrow + the `+` number, are both shipped
(`ScoreChangeChip.swift:58–72`, with `ScoreChange.chipLabel` at
`ScoreChange.swift:98–103`). The two rows below are what is left.

⚠️ **On every card, on both Today and Insights.** *"Make this a requirement,
update the card building skill/rules."*

- `B15-1` ✅ `w2` `chrome` `mech` `gate:none` `ask` — **The steady state must read "Stable", in grey, with a flat arrow**
      **No change** → currently `= no change =`; must become a **flat arrow
      reading "Stable", in grey**.

      ⚠️ Confirmed against the code: the steady case uses the `equal` glyph and
      the words "No change" (`ScoreChangeChip.swift:58–72`,
      `ScoreChange.chipLabel` at `ScoreChange.swift:98–103`). Glyph and string,
      one file.

- `B15-2` ✅ `w3` `chrome` `build` `gate:none` `ask` — **The fourth state, "Learning trends", is not rendered at all**
      **Not enough data yet** → **"Learning trends", in grey**.

      → this pairs with `CoverageGate` (D46) for the "Learning trends" state.

      ⚠️ Not a copy change — there is no else branch to put the copy in.
      `DashboardView.swift:332–334` is an `if let` with no `else`, so the state
      never appears. Upstream, `AppModel.scoreChange(for:)`
      (`AppModel.swift:1287–1299`) collapses **three distinct causes into one
      `nil` and discards the reason**.

      ⚠️ And `CoverageGate` does not already supply this, contrary to how
      `activeContext.md:59–60` reads. Its only producers are
      `EffortIntensityModel.swift:112` and `CardioTrajectory.swift:27`, both
      section-level, and nothing in the app target references it. CoverageGate
      supplies the *sentence shape*; the plumbing out of `ScoreChangeReader` is
      new work.

---

## §B16 — Card title, subtitle and description

- `B16-1` ✅ `w2` `chrome` `mech` `gate:none` `ask` — **Title (line 1) is constant**
      **Title (line 1)** — constant. Never changes unless the card is renamed
      ("Readiness", "Sleep").

      ⚠️ Probably already true — every `InsightResult` is constructed with
      `title: title` from the model's own constant. **Check and mark** rather
      than build.

- `B16-2` ✅ `w2` `chrome` `mech` `gate:none` `ask` — **Subtitle (line 2): words only, no numbers**
      **Subtitle (line 2)** — **words only, no numbers.** The reader's example:
      the Energy card currently reads *"10 · Drained"* and should read
      **"Drained"**.

      ⚠️ A one-line fix, not a sweep. `Energy.swift:538`
      (`headline: "\(Int(output.level.rounded())) · \(output.band)"`) is the
      **only** interpolated-number headline anywhere in InsightKit. Every other
      card's subtitle is already words only.

- `B16-3` ⬜ `w3` `chrome` `design` `gate:decision` `ask` — **Description (line 3): an AI summary of the whole card**
      **Description (line 3)** — *"a non technical, high level summary of the
      entire card's contents. Use AI. It should highlight the most important
      overall trend, the number one thing they should take away if they read the
      whole card."*

      ⚠️ Needs a ruling before any code: whether it runs on-device via
      Foundation Models or elsewhere, and **what it is permitted to say**. This
      is the app's most claim-bearing copy — a free-text AI summary of a health
      card is the one surface where "modelled is never dressed as measured" and
      "every estimate states its error" are hardest to hold.

---

## §B17 — The top section inside a card

- `B17` ✅ `w2` `chrome` `design` `gate:none` `ask` — **Score bubble, title, sub-menu and trend chip in the card's top section**
      **Bug**: the score bubble contains the score *and* the sub-menu text, and
      *"often that sub menu text goes outside the bubble boundary and breaks the
      effect."*

      **Required**: title and sub-menu both sit **to the right of the score
      bubble** — matching how the card looks on the Today and Insights lists.
      Today that space inside the card is empty.

      Also put the **card trend indicators inside the card**, as on the main
      card.

---

## §B18 — Sleep card: five sections, each standalone

⚠️ Three of these are currently nested inside `sleepNightCard`, which
`card-map.sh` reads with a **4,000-character window** and which **fails open**
(backlog finding, 2026-08-06) — splitting them out should *help* that, but the
generated block must be eyeballed rather than trusted to the exit code.

- `B18-1` ✅ `w3` `sleep` `build` `gate:none` `ask` — **Sleep apnoea indicator — dedicated, and it contains "Breathing during sleep"**
      New section. Dedicated, and it **contains** "Breathing during sleep".

- `B18-2` ✅ `w3` `screentime` `build` `gate:none` `ask` — **Screen time impact — dedicated**
      New section. Dedicated.

- `B18-3` ✅ `w2` `sleep` `mech` `gate:none` `ask` — **Last night in stages must become standalone**
      Currently nested. Must become **standalone**.

      One of the three un-nestings that fix `card-map.sh`'s fail-open window on
      `sleepNightCard`.

- `B18-4` ✅ `w2` `sleep` `mech` `gate:none` `ask` — **Your fortnight — its own section**
      Currently nested. Must become its own section.

      One of the three un-nestings that fix `card-map.sh`'s fail-open window on
      `sleepNightCard`.

- `B18-5` ✅ `w2` `sleep` `mech` `gate:none` `ask` — **How fast you fell asleep — its own section**
      Currently nested in the night card. Must become its own section.

      One of the three un-nestings that fix `card-map.sh`'s fail-open window on
      `sleepNightCard`.

---

- `B18-6` ✅ `w3` `sleep` `hard` `gate:none` `ask` — **"What's impacting your sleep" — a bespoke section built from other cards' derived outcomes**
      **The reader, 2026-08-07:** *"I want a dedicated bespoke section for 'what
      is impacting your sleep' and mainly focus on how work, stress, anxiety,
      food, Screen time and other major life factors are impacting sleep.. this
      is where we can finally start using the derived outcomes of cards to build
      new cards and sections like this."*

      ⚠️ **This is the payoff for standing rule 10** — every figure a card works
      out became a `DerivedSeriesID` on 2026-08-06 across all 18 cards, and
      nothing has yet *consumed* that pool to build something new. This section
      is the first thing that would. `DerivedSeriesStore.value(_:on:)` answers
      "what did card X's figure read on day D", which is exactly the join a
      cross-card association needs.

      Candidate inputs, all already derived: work impact's `workExposure` and
      its four quadrants, stress load's channels, mental health's signed
      departures, screen time minutes, `dietaryEnergy` and caffeine, the
      substance log, the calendar's `loadHours`, and the holiday ledger.

      ⚠️ **The statistics are the hard part and this repo has already been burnt
      here.** An independent review refuted the substance card's confirmation
      design on this reader's own record: `heartRate`'s apparent effect fell from
      min|z| 0.91 to **0.03** once same-day step count entered the model, the
      permutation null ran ~2× anti-conservative, BH was invalid under measured
      negative dependence (r = −0.795), and the whole finding set flipped on the
      day boundary. **An association section that does not handle same-day
      activity, the day boundary and multiplicity will produce confident
      nonsense.** Standing rule 1: honest version, always — including "nothing
      has happened often enough to tell it from an ordinary run".

- `B18-7` ✅ `w3` `sleep` `build` `gate:none` `ask` — **A sleep debt section**
      **The reader, 2026-08-07:** *"I also want a sleep debt section."* No
      further detail given.

      What is already there to build on: `SleepNights` and the sleep metrics
      (`sleepDurationHours` 249 rows, `sleepEfficiency` 249, stages, latency),
      and `EnergyModel` already reasons about homeostatic pressure — so the
      published two-process framework the Energy rebuild (`B19`) is researching
      is the same literature this section needs. **Build them against one model
      rather than two**, or the app will carry two different answers to "how far
      behind are you".

      ⚠️ **Needs its own decision before code**: sleep debt against *what*
      baseline — a published need, the reader's own habitual duration, or the
      duration on days they reported feeling best (which is `B18-8`'s subject)?
      Those give different numbers and only the last is personal.

- `B18-8` ✅ `w3` `sleep` `hard` `gate:none` `ask` — **An ideal sleep timeframe section**
      **The reader, 2026-08-07:** *"I also want an ideal sleep timeframe section,
      which will compare over a very long period the days you feel best vs the
      times you went to sleep. I think oura does this."*

      The strongest of the three because it is **personal by construction** — it
      asks what bedtime precedes the reader's own best days, rather than
      restating a published bedtime. `sleepOnset` has 240 rows and the fitted
      centre already exists (`fittedCentre`, used by "Your fortnight").

      ⚠️ **"Days you feel best" has no direct measurement, and choosing its proxy
      IS the design.** Candidates: next-day Readiness, the Energy level, the
      reader's own feedback ledger, or a symptom-free flag. Each answers a
      different question and the section must say which it used.

      ⚠️ **Confounded by the weekend.** Later bedtimes cluster on days followed by
      no alarm, so a naive "late nights precede good days" finding is a
      day-of-week artefact. Social jetlag is already modelled for "Your
      fortnight" — reuse it rather than rediscovering it.

      ⚠️ **Long period means the reader's history is the limit**, not a choice:
      state the span the answer rests on and its uncertainty, per standing
      rule 1.

## §B19 — Energy card accuracy

- `B19` ⬜ `w1` `energy` `ultra` `gate:none` `ask` — **The Energy card has no calibration — five invented constants set its whole range**
      **Re-scoped by the reader 2026-08-07**, when asked what looked wrong:
      *"I Just want more data sources to go into it, plus go and research the
      best way to calculate this. I want to use every possible data source that
      we have in our app, that is applicable to energy. Like.. every
      substances.. they can help with energy.. they should go into this, but
      short-lived, we just need to learn how substances do or do not impact me
      (by substance type)."*

      ⚠️ **The research answered it, and the answer is worse than a miscalibration.
      The card is not miscalibrated — it has no calibration.** Five constants in
      `Energy.swift` set its entire dynamic range and **none of them appears in
      any literature**:

      | Constant | Line | What it decides |
      | --- | --- | --- |
      | `fullChargeSleepHours = 8.0` | :133 | 4 h vs 8 h moves the morning charge **37.5 points** |
      | `minimumMorningCharge = 25.0` | :136 | the floor |
      | `recoveryPointsPerSD = 8.0` | :140 | ±2 SD of overnight HRV moves **16 points** |
      | `fullDrainActiveKilocalories = 1100` | :144 | 550 kcal drains **50 points** |
      | `fullDrainExertionHours = 8.0` | :147 | 4 h above resting drains **50 points** |
      | `trickleRechargePerHour = 2.5` | :153 | a quiet 8 h returns **20 points** |

      Two are checkable against evidence and both come out badly:

      - **Sleep duration is roughly 4× too strong.** The only within-person
        effect size found (Kuula/Bauducco 2025, n = 205, 4,868 actigraphy
        nights) is β = −0.19 KSS per hour of sleep. Four fewer hours ≈ 0.76 KSS
        ≈ **9.5 points** on this scale. The card moves **37.5**. (Adolescent
        sample — indicative, not a calibration, and it is still the only
        within-person number available.)
      - **HRV moves the card 16 points on an association nobody has found.**
        Smyth/van Berkel 2023 (n = 8, 125–386 paired observations each) found
        wearable HRV predicted subjective *vigor* in **0 of 8** participants.

      ⚠️ **And the finding that reframes the ask: more sources cannot fix it.**
      The honest resolution of a published alertness curve on a 0–100 scale is
      about ±21 points, while the entire diurnal swing of a well-slept day is
      about 26. The model's own residual dominates the error budget, so adding
      inputs narrows nothing.

      ⚠️ **The proposed replacement was REFUTED — see `docs/energy-design-2026-08-07.md`,
      which carries a not-build-ready banner.** Three hostile reviewers returned
      `needs-rework`: a fabricated statistic in its own headline, substance
      thresholds understated ~2× by dividing a paired effect by the wrong SD, no
      multiple-comparison policy over ≥20 tests, two of four bands unreachable
      under its own parameters, and a weight-bearing input set of sleep timing
      alone — which **turns the number into a clock**. It also rebuilt the
      shipped `SubstanceImpactInsight` without noticing.

      **So the next step is a re-design, not a build.** What must hold: the
      published model supplies the parameters; the error bar is printed and
      derived; measurements taken after waking must carry weight or the card is
      a clock; and the per-substance layer is a published prior with the
      reader's own episodes plotted against it plus a counter saying how far
      short their record is — **not** a claim at n≈4.

## F. Device-gated — need the phone, not the simulator

- `F1` ⬜ `w1` `devices` `build` `gate:phone` — **Resting Heart Rate page cross-device defect**
      *"Resting Heart Rate page cross-device defect"* — one of the nine things
      that need the phone, not the simulator.

- `F2` ◐ `w2` `charts` `mech` `gate:phone` — **Body Composition after the hatch change**
      *"Body Composition after the hatch change"* — the hatch-never-blend
      encoding shipped; whether it reads correctly with the reader's own body
      composition series is a device question.

- `F3` ◐ `w2` `sleep` `mech` `gate:phone` — **Split-night proof from the next export**
      *"split-night proof from the next export"* — the fix is in; the proof is a
      fresh export off the phone, not a simulator run.

- `F4` ⬜ `w2` `pipeline` `mech` `gate:phone` — **The ingestion pipeline**
      *"the ingestion pipeline"* — only real HealthKit data exercises it end to
      end.

      ⚠️ A stale export is itself a source of false ingestion bugs: the seed
      export `~/HealthSeed/exports/health-insights-export.json` was written
      2026-08-04, one day before the gait-identifier promotion commit
      (`3853446`, HealthKitService.swift:175-178), so its 27,248 walking-speed
      rows sit in `unmodelled` and can never appear in `samples`. Take a fresh
      export before diagnosing the pipeline.

- `F5` ⬜ `w2` `newcards` `mech` `gate:phone` — **The cards on the phone**
      *"the cards on the phone"* — every card seen with real data, which is the
      only thing that falsifies an empty state or a filtered-off card.

- `F6` ◐ `w2` `newcards` `mech` `gate:phone` — **Phase 1 and Phase 2 sections**
      *"Phase 1 and Phase 2 sections"* — built, not yet device-verified.

- `F7` ◐ `w2` `norms` `hard` `gate:phone` — **Heart Health on a young profile**
      *"Heart Health on a young profile"* — a second look at Heart Health's new
      section on a young profile, which is where its bands are most likely to
      read wrong.

- `F8` ⬜ `w2` `screentime` `mech` `gate:phone` — **Screen Time bar measurement**
      *"Screen Time bar measurement"* — the bar can only be measured against the
      real Screen Time figures on the device.

- `F9` ◐ `w3` `capture` `build` `gate:phone` — **The share-sheet action extension (parked on signing)**
      *"the share-sheet action extension (parked on signing)"* — parked, not
      abandoned. The reason it is parked is written up in `docs/deployment.md`
      §"The share-sheet action extension is parked, and this is why", and the
      revert is `c141fc3`.

---

## G. Standing rules you have set

**These are not tasks. They constrain every future task.**

0. ⚠️ **"Ground breaking and unique — do stuff that others will not do."**
   (2026-08-06) *"I do not care about compliance at this point, it's my personal
   project."* **Compliance, clearance and privacy-of-a-public-repo are no longer
   reasons to refuse a feature** — they are reasons to word it carefully and to
   print its error. Thin data means show the error bar, not show nothing. **A
   permanent null is not the safe option; it is the useless one.** If this app
   ever gets a second user, §B5's original reasons all come back.
1. **"Honest version, always."** (2026-08-05) — unchanged by rule 0, and the
   thing that keeps rule 0 from being recklessness: modelled is never dressed as
   measured, and every estimate states its own uncertainty.
2. ⚠️ **Every card shows, even with no data — and NO card is EVER hidden for
   want of data.** (2026-08-07, the reader's words: *"No cards should ever be
   hidden if there is not enough data."*) An empty card says what it is waiting
   for. `InsightResult.isWorthShowing` is unconditionally `true`; the filter it
   used to be is quoted in its own doc comment, and the reasoning that filter
   rested on — *a card with no number earns its place only when there is
   something the user can do about it* — **was superseded by `CoverageGate`**,
   which lets a card name what it is counting to. *"Two more trips"* is a reason
   to keep going; an absent card is not a reason at all.

   **This cost a card the day it was ruled.** Travel drain was fixed to stop
   telling a reader with a connected calendar to connect their calendar; the fix
   correctly set `invitesInput: false`, every term of the old filter went false,
   and the card vanished. The reader found it within the hour.

   ⚠️ **The obligation moved rather than vanished:** visibility is free now, so
   **an empty state that says nothing is the defect to guard against** —
   `CardVisibilityTests.testNoCardLeadsWithADeadEnd` is what holds it.
3. **Every chart carries the substance shading.**
4. **Everything a card charts carries a weight** — or says why it cannot.
5. **Every card gets a bespoke section** (2026-08-06) — creative authority granted.
6. **A fill's colour ramp follows the quantity's own axis** — the radial rule.
7. **Push to `main`. No pull requests.**
8. **Before writing "already arriving" about a data source, count its rows in the last 90 days.** (2026-08-05, learnt the hard way)
9. **Every data entry carries a "what this is" description** (2026-08-06) —
   *"I like how you added a 'What breathing disturbance index is' section, to
   that specific data card. I want that kind of description on EVERY data entry,
   make this a requirement everytime we add a new data type."* Enforced, not
   remembered: `MetricExplainer.explanation(for:)` returns a **non-optional**
   `MetricExplanation` over an exhaustive switch, so a new `MetricType` does not
   compile without one, and `MetricExplainerTests` fails a stub, a placeholder,
   or two halves that say the same thing. **This overturned a written rule** —
   thirty-six metrics used to return `nil` on the argument that explaining a
   step is condescension; the superseded reasoning is kept in that function's
   doc comment. See `docs/data-conventions.md` §4.
10. ⚠️ **Every figure a card works out is a data source, and is used** (2026-08-06)
    — *"the metrics we are deriving from each card, are still not being turned
    into their own individual data sources, and used, especially in weightings.
    E.g. in Biological age card, we created a 'Combined' score, that now should
    be a score that gets its own data row."* And, when the first pass covered
    only the three cards named: **"Do this for EVERY card, and make it a rule
    for every card going forward."**
    For every non-metric quantity a model computes it must declare **either** a
    derived series **or**, in a comment at the site, why it is a pass-through —
    the reader's own exception being *"unless that was just directly derived
    from one other data point"*, which is refused precisely because it would put
    the same number in the Data tab twice.
    A declared figure becomes a `DerivedOutput` (trendable, its own page under
    Data ▸ Generated insights) **and** a `ScoreFactor` with
    `.derived(DerivedSeriesID)` (so it appears in "What goes into this" and "How
    this is weighted", and links through to that page). Most carry weight 0 —
    a figure summarising the rows below it cannot also take a share of them —
    and every weight-0 row states why, which is rule 4 extended to figures a
    card *produces* rather than reads.
    Enforced, not remembered: `DerivedFactorIdentityTests` fails on any derived
    weight whose id names a series the card does not produce. The three verdicts
    and the two borderline calls are in the `add-insight` skill §5a; the section
    behaviour is in `docs/card-sections.md`. **There is deliberately no test
    demanding every card have one** — Readiness and Heart Health honestly do
    not, and such a test would be answered by inventing a figure.
11. ⚠️ **Every quantity the app holds or derives reaches the export** (2026-08-06)
    — *"for things that have no research, we are going to do the research and
    find the 'norms' ourselves… we need to build this into the export mechanism,
    all the data points so when we combine it all at a server-level later, we can
    build these baselines and norms and global trends."*

    For most of what this app derives **no published norm exists**, and the app
    is being built to measure one. The export is the only route from a phone to
    a server-side pool, so **a quantity missing from the export is a quantity
    that can never become a norm.** The standing question changes from *"is
    there a published norm for this?"* to **"is this recorded in a form that
    could become one?"**

    **"It is recomputable" is not an exemption.** Recomputability is a property
    of the device that still holds the raw data. That argument kept derived
    series out of the export and was reversed the same day; the superseded
    reasoning is kept in `HealthDataExport.exportKey(for: .generatedInsights)`.
    This is the export half of the derived-series rule above: that one makes a
    card's figures into data sources on the phone, this one gets them off it.

    Enforced, not remembered — and in two places, because a key can exist and
    be empty (that was D39):
    `HealthDataExportTests.testEveryDataDomainsKeyIsPopulatedOnAFullyPopulatedExport`
    decodes the payload and fails an empty key; `scripts/verify.sh` reads the
    parameter labels off `HealthDataExport.init` and fails when
    `DataExportView.buildFullExport()` does not pass one. Both skills
    (`add-metric-type`, `add-data-or-input`) now ask for the export, not only
    the Data tab.

    ⚠️ **Unchanged by this: the app still never shows a norm it does not have.**
    Collecting toward a future norm is not permission to invent a present one.
    And **nothing is sent in this build** — `NormContribution` is the shape a
    pool would receive (cohort, week bucket, summaries, no free text, no dated
    series) and there is no transport for it anywhere. See
    `docs/norms-and-telemetry.md`.

### The checks these rules still lack

The rules above are constraints, not items. These rows are the *checks* that
would stop each one being held by memory alone. Rules 3, 9, 10 and 11 already
name their enforcement in their own text and need nothing here.

- `G-check-1` ✅ `w0` `rules` `mech` `gate:none` — **A check for rule 4: everything a card charts carries a weight, or says why it cannot**
      Rule 4 is the only one of the enforced-shaped rules with no check behind
      it. Rule 10 extends it to figures a card *produces* and has
      `DerivedFactorIdentityTests`; rule 11 has two checks; rule 3 has
      `verify.sh`. Rule 4 itself — everything a card *charts* — is held by
      memory.

      ⚠️ The shape to enforce already exists rather than needing inventing: the
      weight-0-with-a-stated-reason convention is written at the contribution
      sites, e.g. HeartAgeAnalyser.swift:164-167 and
      MetabolismInsight.swift:396-398.

- `G-check-2` ✅ `w0` `rules` `mech` `gate:none` — **A lint for the D19 class: a hard-coded count inside reader-facing copy**
      A section's copy said *"All four"* on a card that was running on three
      signals. That instance was fixed and pinned with a test. **No lint exists
      for the next one** — nothing stops a new string spelling out a number that
      a later commit changes underneath it.

- `G-check-3` ✅ `w0` `rules` `mech` `gate:none` — **Close rule 5's unenforced half: a bespoke section that is present but empty**
      Rule 5 is only half enforced. `InsightDetailView.swift:79-160` is
      exhaustive over all 26 `InsightID` cases, so a new card cannot ship
      without *a* branch — but an `EmptyView` satisfies the compiler. Readiness
      is the single deliberate `EmptyView`, with its reason documented (its
      picture is the seventeen-vital strip, drawn universally). Nothing
      distinguishes that deliberate one from an unwritten section.

---

## H. Imported from the roadmap — work that lived only in progress.md

These rows were moved here on 2026-08-07, when the three lists became one. Everything below existed **only** in `docs/progress.md` — either in the "Outstanding from the 2026-08-04 Mac session" numbered tables (ids `P<n>`) or in the generated roadmap table (ids `R<n>`, keeping the table's own row number). Every other open row in that file already had an equivalent here under some id and was not duplicated: the cycle phases map to §A3 and §B5 #31, the food and supplement rows to Q7/Q8/Q22 and I8, the crowd-norm rows to §A4 and D42, the integration rows to I2–I7 and I9, the thirteen device-verification rows to §F, and the travel-drain / work-impact / stress rows to §B2 #15–#18 and N1. Where a row was stale, the correction is marked ⚠️ inside its prose rather than replacing what progress.md said — the original wording is what a later session will search for.

- `P5` ✅ `w3` `pipeline` `build` `gate:none` `ask` — **Apple Health `export.xml` converter**
      Scope confirmed: keep everything. The *app's own* export loads today
      (`32e6ce1`); Apple's raw XML does not.

- `P15` ✅ `w3` `bp` `hard` `gate:none` — **BP accuracy section + 30-day projection**
      Headline done (`f6c85ae`). Missing: `HoldOutCheck` series type (the residual
      series already exists inside `drift()` and is discarded), capture-at-sync,
      ledger fields, and the projection.

      ⚠️ **Fix the estimator's "current resting HR" input first** — it is a
      ~2-year mean, so the estimate is near-constant by construction, and any
      ledger recorded before that grades a constant.

      Where the diagnosis is: `~/HealthSeed/research/card-defect-diagnosis.md`.
      Confirmed still open 2026-08-07: no `HoldOutCheck` symbol exists anywhere
      in the tree.

- `P16` ✅ `w3` `substances` `build` `gate:none` `ask` — **Substance card, three parts**
      Score everything currently "charted, not scored"; a recovery-time section
      (single vs several close together vs a big weekend); and a per-substance
      good-vs-bad section.

      ⚠️ Real data is 16 events, 15 stimulant + 1 cannabis, in ~25 days →
      **4 exposure episodes**. A per-substance stimulant panel will near-duplicate
      the pooled card; cannabis at n=1 is unattributable and must show an honest
      empty state.

      ⚠️ Distinct from Q1, which rules on *how the pooled card words itself* at
      three episodes. This row is the three sections that do not exist.

- `P20` ✅ `w3` `charts` `build` `gate:none` `ask` — **Web time-slider + DeepDive**
      Banding, grey underlay, dots and the legend are done. The morph slider with
      granularity selection, and renaming "Over time" → DeepDive with life-wide
      trends, are not.

- `P22` ✅ `w2` `sleep` `build` `gate:none` — **Timeframe-aware sleep chart**
      The Oura time axis (`f971541`) and Apple stages (`8ab542e`) are fixed. The
      third part shipped 2026-08-07: per-stage averages across sources obeying the
      page timeframe, drawn by `SleepStageAverageChart` as Sleep's "A typical
      night" section.

      ⚠️ This sentence used to end *"is not started"* and survived the ✅ for a
      day — agents are told not to edit this file, and the marking pass only
      changed the glyph. **A ✅ row whose body still says "not started" is how a
      closed list starts lying.** The handover's re-read step caught it.

- `P23` ⬜ `w4` `rules` `ultra` `gate:none` `ask` — **Signal audit**
      Every card × every available signal, with justified weightings, against the
      45 modelled types and 320,913 raw rows.

- `P24` ✅ `w4` `transparency` `hard` `gate:none` `ask` — **Model accuracy screen**
      Per-card prediction-vs-actual over time. Tracked in progress.md as one item
      numbered "#24/25".

      ⚠️ Broader than §B5 #36, which shipped the radar's *own* false-alarm
      scorecard (`44fb94e`). This is every scored card, graded against what
      actually happened.

- `P27` ✅ `w1` `pipeline` `hard` `gate:none` — **Ingestion defects — two of four remain**
      ~~Duplicate feeds~~ (`525582c`). ~~Exact zeros as "missing"~~ —
      **re-scoped, see P36.** Still open: **sleep fragments** (median ~11 min
      alongside full nights; filtering cuts HR noise 21%, sleep-efficiency noise
      59%; `SleepSegment.Kind` at `SleepNights.swift:17` is a *stage* vocabulary —
      `case core, deep, rem, unspecified, inBed, awake` — with no fragment
      concept, and `guard asleepSeconds > 0` at `:140` is the only substance floor
      on the whole path), and **cross-device averaging**
      (`VitalReader.swift:153-161` pools per-source day buckets through
      `Baseline.mean`; line 138's own comment says "merged across devices".
      Confirmed unrefuted by two independent verifiers, and **visible on the
      device** — see the Resting Heart Rate detail note below).

      ⚠️ **Corrected 2026-08-07 against the code: the cross-device half is fixed,
      so one of four remains, not two.** `VitalReader.dailySeries` now ranks the
      per-source series by window coverage, then recency, then source name, and
      returns a single winner — "Never a blend" — with the measured evidence for
      the change written into its own doc comment at `VitalReader.swift:172-200`
      (36.5% of 211 resting-heart-rate days carry more than one source; the
      respiratory-rate reference SD is 1.77× wider pooled; `isLeaning` disagreed
      on 7.3% of day-metric pairs). The surviving `Baseline.mean` at `:254` is a
      defensive within-source collapse, not the cross-device mean. **The sleep
      fragment half is unchanged** — `SleepSegment.Kind` at `SleepNights.swift:17`
      still reads `case core, deep, rem, unspecified, inBed, awake`.

      Where the diagnosis is: `~/HealthSeed/research/illness-detection.md`.

- `P31` ✅ `w3` `data` `build` `gate:none` — **New/Deprecated data types**
      **Not derivable today.** No import clock exists on samples; `FieldCatalogue`
      stamps with the *sample's* date and its plumbed-in `now` is never used.
      Deriving "new" from earliest sample date is wrong for 202 of 203 identifiers
      (every connector backfills). "Deprecated" is self-erasing because rolling
      windows drop quiet identifiers entirely. Needs a `TypeSightingLedger`
      sidecar seeded with `seededFromHistory`.

- `P32` ✅ `w4` `capture` `build` `gate:none` `ask` — **Event confirmation feed**
      The app flags an event ("heart rate spiked 30 mins this evening — sexual
      activity?") and the reader confirms or corrects. GPS map, time, why it was
      flagged.

      ⚠️ **Location is a new permission surface and a serious privacy decision —
      needs an explicit ruling before any code.**

      ⚠️ **That ruling has been given and must not be re-asked** — §A2 Q6:
      *"Just build the whole thing."* Feed and prompt together, with an onboarding
      step that explains why before prompting, and a dismissible front-page
      suggestion when the permission is absent. Q6 records the decision; this row
      is the only place the feature itself is specified.

- `P33` ✅ `w3` `transparency` `build` `gate:none` `ask` — **In-app Research section**
      Why each card decides as it does. Source: `docs/research-notes.md`
      (publishable) only.

- `P36` ✅ `w1` `pipeline` `hard` `gate:none` — **Exact zeros as "missing" — the fix needs rework before building**
      One temperature series uses exact 0 for missing on 26% of records. A
      verifier refuted the obvious fix: keying on the **unit** (`degC`) is wrong
      because `HKQuantityTypeIdentifierWaterTemperature` is in the same raw lane
      (`HealthKitService.swift:146`) and a 0 °C cold plunge is a real reading;
      because `store.preferredUnits(for:)` returns the *reader's* preferred unit,
      so the key breaks under a Fahrenheit preference; and because Oura's
      `temperature_deviation` raw rows carry `"°C"` (`MetricType.swift:257`) where
      zero means *at baseline* and censoring it would be the same dishonesty
      pointed at a different series. Key on **identifier**, not unit.

      Also unscoped in the original plan: `RawMetricGroup.latest` is rendered at
      `DataTabView.swift:473` and `DataExportView.swift:338` and would still
      read 0.

- `P38` ✅ `w1` `pipeline` `hard` `gate:none` — **`VitalReader.reading` has no reference gap**
      **Half fixed 2026-08-05 (`9c22745`).** The dominant term was not the gap but
      the *spread*: a standard deviation has a breakdown point of zero, so one
      excursion inflated it and the same value fell back under the bar without
      moving. The flagging path measures with median/MAD now. ⚠️ **The gap itself
      is still absent** — only today is dropped — and the robust scale removes the
      dominant term, not that one. Also: robustness is opt-in, because applied to
      *scoring* it made `ReadinessScore` fall across a month of improving HRV.

      The open half, recorded separately in the same table: **`VitalReader.reading`
      has no reference gap at all.** The radar has a 4-day one
      (`referenceGapDays`); `VitalReader.reading` drops only today, so on **every
      other card** yesterday's excursion is inside the baseline judging today.
      Whether that is right is per-card — a trend card may want it — but it is
      currently accidental rather than decided.

      ⚠️ **Corrected 2026-08-07 against the code: the machinery now exists and the
      decision is what is left.** `VitalReader` has a `window(gap:)` at
      `VitalReader.swift:90-105` with the reasoning written at `:77-88` (the reader
      caught the defect twice on 2026-08-05 — *"still the same value.. but no
      longer in danger?"*). It is **zero by default** and taken only when the
      window can still clear its own minimum without those days, deliberately, so
      that a caller asking "how does today compare with the recent past" does not
      silently lose its newest days. So the row's own framing survives exactly:
      which callers pass a gap is still per-card and still undecided.

- `R16` ◐ `w4` `capture` `build` `gate:none` `ask` — **Camera-based input (AI + LiDAR) is still the open half**
      The open half of the master add button (user request, 2026-08-02): test
      results beyond the shipped blood-test photo, body-composition scans, food
      for nutrition reporting.

      Scoping notes for the open half:

      - **Existing pieces it unifies**: blood-test photo import (shipped:
        on-device OCR → confirmed grounding values), the VisionKit live
        scanner (open, below), Foundation Models lab-analyte extraction
        (open, below), the LiDAR body scan (roadmap note below — ARKit,
        Pro-only, un-exercisable from a sandbox), and Body Composition's
        deferred "view & add" scan entry (gap 10 in `docs/card-sections.md`),
        which becomes a route in this menu rather than its own build.
      - **Food/nutrition is the genuinely new territory**: no `MetricType`
        exists for any dietary quantity, diet is the one absent LE8
        component (see `activeContext.md` ▸ the Life's Essential 8 note),
        and camera-based food recognition plus quantity estimation is an
        accuracy claim that needs the same honesty framework as the BP
        estimator — provenance, confidence, and its own metric rather than
        merging into anything. Nutrition metrics feed no card yet; per
        "a metric with no reader is invisible", the first food build should
        include at least one reader (Energy's intake side is the natural
        candidate).
      - **Camera + AI capture is device-only** end to end (VisionKit, ARKit,
        FoundationModels) — CI compiles it, only the phone can exercise it.
        Build the menu and manual routes first from the sandbox; land the
        capture routes in device-verifiable slices.

      ⚠️ Its two remaining deliverables are already tracked as separate rows —
      I8 (barcode/food) and I9 (LiDAR body scan). This row is kept for the
      scoping above, which appears nowhere else: the "one menu, not its own
      build" ruling on gap 10, and the reader requirement on the first food
      build. The dietary-quantity claim is now stale — ten macros plus eleven
      micronutrients were promoted on 2026-08-03/05.

- `R20` ✅ `w3` `bodyscan` `build` `gate:none` — **A 3D mesh instead of the silhouette**
      `BodySilhouetteView.outline` is static and pure so a mesh can replace it
      without touching anything above.

      ✅ **Verified done 2026-08-07.** `BodyOverTimeSection.swift:44` renders
      `BodyMeshView(mesh: BodyMeshBuilder.mesh(for: shown.parameters))`, and
      `BodySilhouetteView` is referenced from nowhere in the app target — the
      purity the row relied on is what made the swap invisible, exactly as
      written. ⚠️ The file is now dead code and should go with the next sweep of
      §E D4's list.

- `R24` ✅ `w3` `substances` `build` `gate:none` `ask` — **The medication panel — still open**
      The card charts `activeMedicationLevel` and states on the row that the
      evidence is about intake rather than expenditure, but the before/after
      contrast shipped 2026-08-07 as `MedicationPanelSection`, reporting the two
      deltas — what intake did, what expenditure did — rather than one number,
      which is the honest shape this row asked for.

      ⚠️ It still needs a logged stretch on each side of the
      first dose, and the honest version reports the two deltas (what intake did,
      what expenditure did) rather than one number. Original scope follows.

      **The medication panel.** Intake and expenditure are both dated series and
      `activeMedicationLevel` is a third, so this is Substance Impact's
      before/after shape with a different pair of quantities. The expected
      honest finding is *"the drug moved what you eat, not what you burn"*.
      **"Mounjaro speeds up your metabolism" is a claim this card must never
      make** — no such effect is established, and a rising ratio during
      treatment is more likely a food log that got worse as appetite fell. What
      it *can* say, and what nothing else in the app can see: observed against
      predicted-for-your-current-size, tracked across the treatment period —
      which is what "my metabolism has slowed" actually means.

      **The trap, which is a rule rather than a task:** do not promote Apple's
      `basalEnergyBurned` as "your metabolism". It is a formula the phone
      evaluated from height, weight, age and sex — the modelled-dressed-as-measured
      failure this app has rules against. A labelled comparator at most.

      ⚠️ The reader's own ask this serves, quoted at the head of the metabolism
      section: *"I'm always wanting to know how fast my metabolism is at the
      moment, and how it's sped up by Mounjaro or similar medications, and how
      it's helping me lose weight — or making it harder."*

- `R25` ✅ `w3` `nutrition` `hard` `gate:none` — **Composition-aware kcal/kg, later**
      Fat is ~9,400 kcal/kg and lean ~1,800 against the blanket 7,700, and on a
      GLP-1 the lean fraction of loss is not negligible. Worth a few per cent,
      once the logging gate is being met.

- `R26` ✅ `w3` `nutrition` `hard` `gate:none` — **The relationships from the reader's own history**
      Protein against lean-mass retention while weight falls, caffeine against
      sleep onset. The card scores against published figures today and says
      nothing yet about what the reader's own data shows, which is this app's
      strongest claim elsewhere. Split out rather than left inside the item
      above, because a multi-clause `[x]` hiding an unfinished clause is how six
      of them once survived a "closed" list.

- `R28` ✅ `w3` `sickdays` `build` `gate:none` — **Reconcile the symptom log against the hand-entered side effects**
      Still open, and it is the half that becomes a card: what the reader logged
      by hand versus what Health already knew, in the same shape
      `BodyMeasurementReconciliation` uses. `isCommonGLP1Effect` and
      `isInfectionLike` are built and disjoint, ready for it.

- `R33` ✅ `w3` `radar` `build` `gate:none` — **Cycle and reproductive health — one real asset, and it is not the cycle**
      `MenstrualFlow` and `SexualActivity` are **zero rows**.
      `BasalBodyTemperature` is **136 rows over 124 days, 80 of the last 90** —
      written deliberately via a Shortcut and read by nothing.
      **Use it as an independent temperature channel for the symptom radar**,
      which is worth far more than a cycle card: it survives a night the ring
      was off, which is exactly the night the radar currently goes blind.
      ⚠️ It must go through the placeholder filter first — 35 of the 136 records
      are exact zeros meaning missing (`RawMetricSample.swift:113`).
      Anything contraceptive needs FDA clearance and is out of scope entirely.

      ⚠️ **Two clauses are superseded; the middle one is the item.** The cycle
      tab shipped (§B5 #31, both slices), so this row is no longer about cycle
      tracking — what survives is the basal-temperature channel for the radar,
      which nothing else in this file asks for. §A3 records `basalBodyTemperature`
      only as a way to *confirm* a cycle, and against **the reader's** export,
      not the wife's ring. And the contraceptive clause is no longer a refusal:
      standing rule 0 retired clearance as a reason not to build, and it survives
      as the wording rule, linted — `CyclePhaseModel.notContraceptionNotice` must
      be on screen in any view drawing a fertile window.

- `R57` ⬜ `w4` `radar` `ultra` `gate:none` — **Core ML personal anomaly detection once enough history exists**
      Recorded in progress.md under "On-device ML" as its whole content — one
      line, no design. Needs a brief before any build: what it would detect that
      `HealthWatchModel`'s calibrated statistic and CUSUM do not, and against
      what personal null, or it is a fourth rendering of the radar's question.

---

## I. Imported from active context — work that lived only there

`docs/activeContext.md` is 4,249 lines and most of its "next session" lists have
been overtaken: §B5 #34/#35 (Fitness sections), #33 (sound exposure), #30
(breathing disturbance) and #31 (cycle slice 2) all shipped, and both hazards
those rows warned about were handled in the same commits — `Feedback.swift`
now returns `"fitness-v2"` (`Feedback.swift:159`, asserted at
`EffortIntensityTests.swift:283`) and the three promoted identifiers were removed
from `HealthKitService.otherQuantityIdentifiers` in the same edit
(`HealthKitService.swift:126–144`, `:196–202`). The "Three things this session's
own work got wrong" section is fully closed too: the CUSUM comment now describes
its code (`HealthWatch.swift:614–615`), `ContributorsFixture.fullCoverage`
defaults to 130 days (`ContributorsFixture.swift:29`), and `roadmap-table.sh` no
longer anchors `- [ ]` at column 0. Four items are genuinely open and recorded
nowhere else — and one correction matters: **the calendar-drift patch is not
lost.** It survives, all 706 lines, under the *abandoned* iCloud checkout's
scratchpad, which `/private/tmp` will drop on the next reboot.

- `AC1` ✅ `w2` `calendar` `build` `gate:none` — **The calendar re-judgement-on-drift patch — 706 lines, apply-ready, and it still exists**
      From `activeContext.md` ▸ "Next session, in order": *"The calendar-drift
      patch is apply-ready — 706 lines with tests, at
      `…/scratchpad/calendar-drift-patch.md` if it survives, otherwise re-derive
      from D-row. It needs one anchor checked in `InsightDetailView.body`: the
      `.onDisappear` must attach at body level, **not** to the section, or a
      `LazyVStack` fires it on scroll."*

      ⚠️ **It survives, and it is on a clock.** It is at
      `/private/tmp/claude-502/-Users-jason-salway-Library-Mobile-Documents-com-apple-CloudDocs-HealthAppLocal-health-insights-ios/54307a14-df60-4743-98d6-7fde0e36b6de/scratchpad/calendar-drift-patch.md`
      — the *abandoned* iCloud checkout's session scratchpad, because the session
      that wrote it was running out of that tree when iCloud revoked access.
      `/private/tmp` is session-scoped and does not survive a reboot. **Copy it
      into the repo before doing anything else with it**; a session that follows
      the docs' "if it survives" wording will conclude it is gone and re-derive
      the expensive part.

      ⚠️ **The anchor is now checked, and the hazard is real.**
      `InsightDetailView.swift:423–429` is `ScrollView { LazyVStack(...) { … } }`
      — deliberately lazy, "a card is eleven-plus sections and several run real
      models". So an `.onDisappear` on `calendarReviewSection` would fire every
      time the section scrolls out of view. It must attach at body level.

      What the patch does, per its own header: it makes
      `CalendarEventArtifact.differs(from:)` act — its doc comment currently says
      *"Nothing acts on this yet"*. Nine parts: add an optional `start` to
      `CalendarEventArtifact` (the one genuine gap; the R3 snapshot itself
      already ships, `ce9f76f`), a defaulted memberwise-init parameter so
      `Sharing.swift:545` and `SharingTierTests.swift:28` keep compiling, a
      needs-re-judgement flag carried through `CalendarEventClassification`
      (`reclassified` carries it, `reviewed` clears it), the persistence field,
      a `DataStore` writer, an `AppModel` queue flushed at the top of
      `syncCalendar()`, the row in `calendarReviewRow`, the boundary
      `.onDisappear`, and a new test file.

      ⚠️ **Belt-and-braces today, load-bearing later**, in the patch's own words:
      `CalendarIntegration.fetchEvents` folds the occurrence's start into the
      identifier (`"\(eventIdentifier)|\(startDate.timeIntervalSince1970)"`), so
      dragging a meeting mints a new `CalendarEvent.id` and the old judgement is
      left behind rather than found to have drifted. Drift detection goes blind
      to the most obvious edit a reader can make the moment that id scheme
      changes.

      ⚠️ **NOT APPLIED, NOT COMPILED, NOT TESTED.** Every edit was designed
      against file contents read that session, but nothing was run —
      `verify.sh --tests` is the gate before this is believed.

- `AC2` ⬜ `w0` `capture` `mech` `gate:external` — **Three things the reader must do on their own devices before daylight or falls can record anything**
      `activeContext.md` ▸ "Next session, in order" #4: *"`docs/watch-capture-setup.md`
      has three things **the reader must do on their devices** before daylight or
      falls can record anything."* This is an action for the reader, not a build,
      and it is not on any list they can act from — it sits only in a research doc
      and a next-session bullet. It gates §B5 #37, whose whole difficulty is that
      those types read **zero rows**.

      The three, from `docs/watch-capture-setup.md`:

      1. **Settings ▸ Privacy & Security ▸ Location Services ▸ System Services ▸
         Motion Calibration & Distance** must be **on** — the prerequisite nobody
         guesses, and why Time in Daylight can read zero on a worn watch.
      2. **Watch ▸ Settings ▸ SOS ▸ Fall Detection ▸ "Always on"** — under 55 is
         not "off": watchOS 8.1+ enables it *only during workouts* for ages 18–55.
      3. **Confirm each is recording** at **Health app ▸ Search ▸ Other Data ▸
         Time in Daylight** and **▸ Number of Times Fallen** (not Mobility — that
         was our wrong prior), and grant this app read access per type at **Health
         ▸ profile ▸ Privacy ▸ Apps**.

      ⚠️ **Every day this is not done is a day of history that cannot be
      backfilled**, which is why it is `w0` rather than queued behind the card.

      ⚠️ **The app can never detect any of this.** A denied or unrecorded read is
      indistinguishable from no data — HealthKit hides read-denial deliberately.
      That is precisely why the checklist has to reach the reader rather than
      being inferred by code.

      ⚠️ Two findings from the same doc that constrain what gets built after:
      **UV Exposure should not be promoted to a `MetricType`** (no Apple device
      has ever written it), and **a dismissed fall is never logged** —
      `numberOfTimesFallen` is written only on a confirmed fall or an escalation,
      so a zero is not evidence of no falls and any card must say so.

- `AC3` ✅ `w0` `rules` `mech` `gate:none` — **Archive sessions 24–27 out of `activeContext.md` — instructed twice, done neither time**
      `activeContext.md` ▸ "⚠️ Docs debt, now overdue": *"`activeContext.md` is
      past 4,100 lines across 29 sessions and is read at every session start. The
      handover protocol has instructed archiving superseded sections since
      2026-08-06 and **it has not been done twice running**. The next session
      should move sessions 24–27 to `docs/archive/activeContext-history.md`
      before adding to it."*

      Measured today: **4,249 lines**, and `docs/archive/` does not exist yet.

      ⚠️ **This is the same work as §E D38, seen from the handover side.** D38
      records the cost ("session-start cost is dominated by
      `docs/activeContext.md`… the first actual pruning needs a main session")
      with a stale 4,135-line figure. What this row carries that D38 does not:
      **which sessions to move** (24–27), and the fact that the instruction has
      now been issued by the protocol twice and ignored twice — which is the
      argument for it being a gate rather than an instruction. Collapse the two
      rows if you like, but keep both facts.

- `AC4` ⬜ `w3` `data` `build` `gate:needs:Q13` — **The delete-everything wipe must be driven by the `Schema`, not a hand-written list**
      A constraint on Q13 that exists only in `activeContext.md` ▸ "The next
      session's build list": Q13 delete-everything *"must come after the cycle
      tab, or the wipe silently misses two new `@Model` types"*. The backlog's own
      Q13 row is one word — *"Yes"* — and carries none of this.

      The ordering half is now moot (the cycle tab shipped), but the hazard it
      names has got worse, not better: `DataStore.init` (`DataStore.swift:13–22`)
      registers **16 `@Model` types**, and **four of them landed after Q13 was
      answered** — `CycleDayRecord`, `CalendarEventRecord`,
      `CalendarJudgementRecord` and `HolidayEntry`. A wipe written against a
      hand-enumerated list is silently incomplete the next time a `@Model` is
      added, and on this feature "silently incomplete" means health data the
      reader was told was deleted.

      ⚠️ The `Schema` array already carries the comment *"A @Model not listed here
      silently never persists"* — the wipe needs the mirror-image guarantee, and
      it should be derived from the same list rather than restating it.
